# Code mort — opencode-proxy — 2026-08-23

> **Branche :** `one-nvpn-align` — commit `4fd3a36` + diff local 4 fichiers pending (23 deletions — 5 orphans lot2)
> **Méthode :** protocole 6 étapes — vulture (fallback ruff F401/F841), coverage --skip-covered, grep imports, grep symbole, git log -S, verdict
> **Date :** 2026-08-23 — refresh 19:00 après lot2 (5 orphans supprimés, ruff 0, pytest 611 passed)

---

## 1. Protocole appliqué

1. `vulture . --min-confidence 80 --exclude .venv,.kilo,logs` → non installé, fallback `ruff --select F401,F841,F821` + `grep` manuel
2. `coverage run -m pytest && coverage report --skip-covered` → non exécuté ce refresh (gate 35% abaissé commit c3965be, à remonter Phase 2)
3. `grep -rn "import.*vpn_manager\|from.*import" --include="*.py"` → imports non résolus
4. `grep -rn "SYMBOL"` + `git log -S SYMBOL` par candidat
5. Verdict : supprimer / conserver / déprécier + pragma
6. Livrable : ce tableau

Commandes reprodu :
```bash
python -m ruff check . 2>&1
# All checks passed!  (vs 28 refresh 14:57, vs 29 avant F823)
python -m pip_audit --requirement requirements.txt 2>&1 | tail -5
# No known vulnerabilities found  (vs 32 vulns refresh 14:57)
grep -rn "free_quota\|free_quota.py" --include="*.py" . | grep -v ".venv"
grep -rn "\[plan\|\[fix\|\[incident" --include="*.py" . | wc -l
wc -l opencode.py vpn_manager.py dashboard/api.py | sort -rn
git diff --stat HEAD | tail -5
# 76 files changed, 1053 insertions(+), 782 deletions(-)
git ls-files --others --exclude-standard | head -30
```

---

## 2. Résultats ruff (2026-08-23 refresh 19:00)

- **Total : 0 erreur — `All checks passed!`** (vs 0 refresh 18:30, vs 0 refresh 15:30, vs 28 refresh 14:57, vs 324 totaux historiques dont 59 faux positifs `__init__.py` re-exports)
- **Répartition :**
  - `E402` module level import not at top : **0** (était 23 — `config/settings.py:1460`, `dashboard/api.py:20,22-27,41,43-46`, `middleware.py:203`, `opencode.py:504-518,2229-2230,2892,5191` — déplacés en tête / `noqa`)
  - `F401` unused import : **0** (était 4 — `opencode.py:3038 curl_cffi.requests.AsyncSession`, `opencode.py:5216 _anthropic_cache, _anthropic_cache_max, _orig_anthropic_to_openai` — supprimés)
  - `F811` redefinition : **0** (était 2 — `opencode.py:5347 THINKING_MODELS`, `opencode.py:5445 _extract_cache_tokens` — dédupliqués vers `protocol_mapping` single-source)
  - `F823` local referenced before assignment : **0** (était `dashboard/quota.py:767 _models_cache` — corrigé `global _models_cache` dans `_poll()`)
  - `F841` unused local : **0** (était 3 — `protocol_mapping.py:1230 has_reasoning_item`, `protocol_mapping.py:1286 has_reasoning_item`, `protocol_mapping.py:1290 has_thinking_block` — supprimés 15:30)
  - `TOOL_CAPABILITIES` : **0 hit** après purge 18:30 (`grep -rn TOOL_CAPABILITIES --include="*.py"` → 0)
  - Lot2 orphans : **0 hit** après purge 19:00 — `UNIVERSAL_TOOLS` 0, `_WORKSPACE_ID_ENV`/`_AUTH_COOKIE_ENV` 0, `_is_string_char` 0, `_key_cycle` legacy 0 (gardé `_key_cycle_keys/_index/_lock` vivants), `_geo_rotation_task` 0 ; `config.yaml:dashboard.universal_tools` 8l supprimé
- `vulture` : non installé — à ajouter Phase 1 (`pip install vulture`)
- `pip-audit --requirement requirements.txt` : **0 vuln** (`No known vulnerabilities found`)
- `pytest -o addopts= -q --tb=line --disable-warnings` : **611 passed, 1 skipped, 4 warnings in 12.02s** (re-vérifié 19:00 — vs 11.60s 18:30)

---

## 3. Tableau verdicts (refresh 19:00)

| `dashboard/api.py:53` | `UNIVERSAL_TOOLS` 7 entrées | `grep -rn UNIVERSAL_TOOLS --include="*.py"` → 1 hit (déf seule) ; 0 import, non ré-exporté `__init__`, 0 usage JS ; `config.yaml:303 universal_tools` 8l orphelin | **✅ Mort supprimé 19:00 — lot2** | `UNIVERSAL_TOOLS = {...}` 3l + `config.yaml:dashboard.universal_tools` 8l supprimés (11l) |
| `dashboard/quota.py:44-47` | `_WORKSPACE_ID_ENV/_AUTH_COOKIE_ENV` | `grep -rn _WORKSPACE_ID_ENV\|_AUTH_COOKIE_ENV` → 1 hit chacun (assignation seule) ; prefix `_` privé ; `quota.py` lit `os.getenv` direct + `config.settings.API_KEYS`, jamais ces const | **✅ Mort supprimé 19:00 — lot2** | `_WORKSPACE_ID_ENV/_AUTH_COOKIE_ENV = "..."` 4l supprimés (garde commentaire `Env var validation` retiré avec) |
| `dashboard/quota.py:365` | `_is_string_char(ch, quote)` | `grep -rn _is_string_char` → 1 hit (déf seule) ; prefix `_` privé ; `_read_object_literal` gère `quote` inline, jamais appelé | **✅ Mort supprimé 19:00 — lot2** | `def _is_string_char... 3l` supprimé — `_read_object_literal` intact |
| `opencode.py:66` | `_key_cycle = None  # legacy` | `grep -rn _key_cycle` → 2 hits (var legacy + `global _key_cycle` 95) ; `_key_cycle_*` vivants (`_key_cycle_keys/_index/_lock`) distincts — legacy jamais lu | **✅ Mort supprimé 19:00 — lot2** | `_key_cycle = None` 1l + `global _key_cycle,` 1l supprimés (2l) — `_key_cycle_keys/_index/_lock` conservés |
| `opencode.py:1420` | `_geo_rotation_task = None` | `grep -rn _geo_rotation_task` → 1 hit (assignation seule) ; jamais lu/écrit ailleurs ; `_geo_breaker/_geo_pin*` vivants | **✅ Mort supprimé 19:00 — lot2** | `_geo_rotation_task = None` 1l supprimé |

## 3bis. Tableau verdicts — lot précédent (refresh 18:30)

| Fichier:ligne | Symbole / pattern | Preuve | Verdict | Action |
|---------------|-------------------|--------|---------|--------|
| `dashboard/quota.py:762` | `_models_cache` sans `global` | `ruff F823` avant fix ; `grep -n _models_cache` 5 occ. dont `_poll()` ligne 767 sans global | **✅ Bug corrigé 2026-08-23** | `global _models_cache` ajouté dans `_poll()` — ruff 29→28→0, pytest 611 passed |
| `opencode.py:3038` | `curl_cffi.requests.AsyncSession` | `F401` → 0 ce refresh (importé mais jamais utilisé) | **✅ Mort supprimé 15:30** | Import supprimé (pool utilise `AsyncSession` via `_get_pooled_curl_session` import local) |
| `opencode.py:5216` | `_anthropic_cache`, `_anthropic_cache_max`, `_orig_anthropic_to_openai` | `F401 ×3` → 0 ce refresh (importés depuis `protocol_mapping` mais 0 usage) | **✅ Mort supprimé 15:30** | Imports supprimés — reste de déduplication F-M7 |
| `opencode.py:5347` | `THINKING_MODELS` redéfinition | `F811` → 0 (défini import puis redéfini 5347) | **✅ Duplication supprimée 15:30** | Redéfinition 5347 supprimée, gardé import `protocol_mapping` single-source |
| `opencode.py:5445` | `_extract_cache_tokens` redéfinition | `F811` → 0 (défini 5197 puis redéfini 5445) | **✅ Duplication supprimée 15:30** | Doublon 5445 supprimé |
| `protocol_mapping.py:1228,1285-1290` | `has_reasoning_item` / `has_thinking_block` | `F841 ×3` → 0 ce refresh (flags assignés jamais lus) | **✅ Mort supprimé 15:30** | `has_reasoning_item = True` ×2 + `has_thinking_block = True` supprimés |
| `config/settings.py:1460` + 22 autres | `E402` late imports | 23× → 0 ce refresh | **✅ Dette style close 15:30** | Déplacés en tête / `noqa` justifié |
| `config/settings.py:1284-1305` | `TOOL_CAPABILITIES` / `load_tool_capabilities()` + `tool_compat_results.json` 18K | `grep -rn TOOL_CAPABILITIES --include="*.py"` → 1 hit (assignation seule) ; `config/__init__.py` pas de ré-export ; `grep tool_compat` → 3 hits dont 1 artefact ; `git log -S TOOL_CAPABILITIES` → 87fe9eb/3e26c18/d723a74 (routage par outil supprimé 3e26c18) | **✅ Mort supprimé 18:30** | `load_tool_capabilities()` 12l + `TOOL_CAPABILITIES = ...` 5l supprimés (−17l, 1514l), artefact `tool_compat_results.json` 18K `rm -f`, `grep TOOL_CAPABILITIES` → 0, `ruff All checks passed`, `pytest 611 passed` |
| `free_quota.py` | Module entier 245 l | `ls free_quota.py` → No such file ; `grep -rn free_quota` → 0 import | **✅ Supprimé** (F-H1 clos) | Déjà supprimé — ne pas recréer |
| `.kilo/` 61M | Dossier hors repo | `du -sh .kilo` 61M ; `grep kilo .gitignore` → `68:.kilo/` présent | **✅ Hors repo — ignoré** | OK depuis fix `.gitignore` 68 — `git status` propre |
| `logs/requests.db.bak-20260822-185029` 3.1G | Backup DB doublon | `ls -lh logs/*.db*` 3.1G + 3.1G + 4.1M WAL + 32K shm = 6.2G total | **Transient** | Supprimer `*.bak-*` >7j, ajouter rotation/VACUUM Phase 1 |
| `dual_station` | `config.yaml:119 dual_station: true` + `station_count: 2` | `vpn_manager.py` gère `station_count` 1–10, `dual_station` legacy bool | **Legacy** | Déprécier `dual_station`, garder `station_count` seul — warning si divergence |
| `server_countries` dupliqué | `config.yaml:138-143` + `docker-compose.yml:35,148,235` + `vpn_manager.py:510` | 36 pays, 4–5 endroits (F-H5) | **✅ Duplication close 15:30** | Source unique `config.yaml:ip_rotation.server_countries` → compose `${SERVER_COUNTRIES:?...}` fail-closed + `scripts/make_credentials_env.py` |
| `gunicorn==22.0.0` | `requirements.txt` | `Dockerfile` lance `uvicorn` direct, `opencode.service` `uvicorn` direct, `gunicorn.conf.py` non utilisé en prod | **Dépendance morte ou doc manquante** | Soit supprimer, soit documenter usage prod `gunicorn` vs dev `uvicorn` |
| `WEB_PORT 8082` vs `PORT 4000` | `config/settings.py` / `config.yaml` | Dashboard même app port 4000, `WEB_PORT` vestige | **Mort / confusion** | Unifier `PORT` seul, supprimer `WEB_PORT` ou documenter |
| `custom_routes.json` | `custom_routes.json` + `config.yaml:custom_routes` | Double source routes (backward-compat) | **Dette compat** | Migration auto au boot + warning, déprécier JSON Phase 3 |
| `api_keys.json` vs `config.yaml:api_keys` vs `.env` | Triple source clés | `ENV_DIVERGENCE` guard existe | **Dette config** | Figer contrat : YAML = source, ENV = secrets only, JSON déprécié |
| Artefacts `logs/_*`, `tmp/`, `pp1.json`, `*_diff_tmp.txt` | `.gitignore` couvre `logs/_*`, `tmp/`, `pp1.json` | `git ls-files --others` n'en liste pas mais présents disque si générés | **Transient** | `git clean -fd` si besoin, ne pas committer |

---

## 4. Quantification (refresh 19:00)

- **Code mort avéré supprimé cumulé :** 4 imports F401 + 2 redéfinitions F811 + 3 flags F841 + 23 E402 triés + **TOOL_CAPABILITIES 17l** + **lot2 23l** (UNIVERSAL_TOOLS 3l + `config.yaml:universal_tools` 8l + _WORKSPACE 4l + _is_string_char 3l + _key_cycle 2l + _geo_rotation_task 1l + artefact 18K) ≈ **~72 l** net + **75 l** `opencode.py` déduplication + **47 l** `protocol_mapping.py` ; core 6 fichiers **21669** (`opencode 10431` + `vpn_manager 4141` + `dashboard/api 3163` + `quota 827` + `settings 1514` + `protocol_mapping 1593`) — total py tracked **42124** (vs 42242 18:30, vs 42259 15:30), avec `test_coverage_boost.py` untracked 73l → **42197** sur disque (`find` 42342 inclut autres non-trackés)
- **Bug corrigé :** `dashboard/quota.py:767 F823` (1 l fix `global`) — ruff 29→0
- **Déjà supprimé :** `free_quota.py` 245 l (F-H1 clos)
- **Hors repo :** `.kilo/` 61M désormais ignoré (was 61M untracked)
- **Transient disque :** `requests.db.bak` 3.1G doublon → **50% de `logs/`** (6.2G total) — à purger Phase 1
- **Dette historique :** 341 tags `[plan`/`[fix`/`[incident` (vs 339 refresh 14:57, vs 342 baseline)
- **Deps :** `pip-audit 32→0` (Pillow 10.4.0→12.3.0, starlette 0.52→1.6.0, curl_cffi 0.14.0→0.16.1, fastapi 0.129.2→0.141.1) — `requirements.txt` 22 l pinné, source unique (lock supprimé)
- **Git diff HEAD :** **4 files changed, 1 insertion(+), 23 deletions(-)** (`config.yaml` 8 + `dashboard/api.py` 3 + `dashboard/quota.py` 9 + `opencode.py` 4/1) — vs 1 file 17 deletions avant lot2

---

## 5. Recommandations par priorité (refresh 19:00)

- **P0 (<2h) :** ✅ **Clos — lot2 inclus 19:00** — F401/F811/F841/E402/F823 tous à 0, `TOOL_CAPABILITIES` 17l + artefact 18K + 5 orphans lot2 (23l) purgés, `pip-audit 0`, `server_countries` source unique, `ruff All checks passed`, `pytest 611 passed`. Reste `find logs -name "*.bak-*" -mtime +7 -delete` (50% logs).
- **P1 (1j) :** rotation DB/VACUUM, `vulture` en CI, `PROXY` SSRF validation, `DASHBOARD_REQUIRE_TOKEN` fail-closed, `control_api_key` hors YAML
- **P2 (1 sem) :** déprécier `dual_station`, `custom_routes.json`, `WEB_PORT`, `gunicorn` — ADR ; centraliser `free_discovery` cascade
- **P3 (2 sem) :** découper `opencode.py` 10431 l — dead code disparaît avec le découpage

*Généré 2026-08-23 19:00 — versionné dans `docs/dead-code-2026-08-23.md` (Phase 0 clos + lot2 5 orphans purge).*
