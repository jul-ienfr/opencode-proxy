# Audit opencode-proxy — 2026-08-23 (refresh)

> **Périmètre :** `opencode-proxy` — proxy FastAPI Claude Code → opencode.ai
> **Branche auditée :** `one-nvpn-align` (base `main`), commit `a9bc334` + diff local 76 fichiers (1053 + / 782 -) — cf. `git diff --stat HEAD` §10.
> **Méthode :** 4 passes — A/ scans automatiques (tailles, grep, ruff, hygiène git, pip-audit), B/ revue 9 dimensions H/M/L, C/ quantification dette & code mort, D/ rapport & roadmap. Outillage : `ruff`, `vulture` (non installé, fallback ruff F401/F841), `grep`, `wc -l`, `git`, `pip-audit --requirement requirements.txt`, `pip list`.
> **Auteur :** audit automatisé (contexte ultracode) — à relire/contresigner humain.
> **Refresh :** 2026-08-23 15:30 — métriques re-mesurées après Phase 0 clos : `ruff All checks passed` (28→0), `pip-audit 32→0` (Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 + fastapi 0.141.1), `server_countries` source unique F-H5 → ✅, `protocol_mapping` F841 ×3 nettoyés, `pytest 611 passed 1 skipped`.

---

## 1. Score global & radar

### Score global : **6,6 / 10** — *Correct, exploitable en prod — Phase 0 clos, P0 sécu restant*

Le projet est **fonctionnellement solide** (perf, infra Docker/nginx/gunicorn soignées, copie explicite, healthcheck, rate-limit, SSE, quotas, rotation multi-station) mais porte une **dette structurelle** (god file 10,4 kl, 341 commentaires historiques, CI permissive, `control_api_key` exposé). Phase 0 quick-wins clos ce refresh : lint 0, vulns 0, source unique pays, god file -75 l.

### Radar 9 axes (0 = critique, 10 = exemplaire)

```
Architecture & couplage  ██████░░░░  5.5
Dette & code mort        ██████░░░░  6.0  (+0.5 — F401/F811/F841 clos, E402 trié)
Qualité & style          ██████░░░░  6.0  (+1.0 — ruff 0)
Antipatterns/robustesse  ██████░░░░  6.0
Testabilité              ██████░░░░  6.5
Sécurité                 ██████░░░░  6.0
Performance              ████████░░  8.0
Infra & déploiement      ███████░░░  7.5
CI / Hygiène & deps      ██████░░░░  6.0  (+1.5 — pin deps + pip-audit 0)
```

| Axe | Note | Raison courte |
|-----|------|---------------|
| Perf | 8.0 | orjson, httpx http2/socks, uvloop/httptools, gunicorn cpu_count, nginx SSE-safe — bien. Tuning pool non mesuré. |
| Infra | 7.5 | Dockerfile pinned, explicit COPY --chown, HEALTHCHECK, nginx buffers complets. Compose 487 l (F-H5 fail-closed `${SERVER_COUNTRIES:?...}`). |
| Testabilité | 6.5 | 38 fichiers tests, ~611 tests verts (1 skipped), fixtures conftest. God file freine la couverture unitaire. |
| Sécu | 6.0 | DASHBOARD_TOKEN HMAC + warning 0.0.0.0, secrets gitignored, COPY explicite. Mais `control_api_key='***REMOVED-ROTATE-ME***'` en clair dans `config.yaml:178` + docker.sock mount + PROXY sans validation — P1 restant. |
| Robustesse | 6.0 | RateLimit/CircuitBreaker pure-ASGI, WAL SQLite sérieux, retry. Mais `threading.Lock + cycle` en async, poll 5s, mmaps 268M sans doc WAL. |
| Archi | 5.5 | God file 10433 l + 4141 l + 3166 l concentrent 78% du code. Couplage config facade 40+ symboles. |
| Dette | 6.0 | 341 tags historiques, .kilo 61M désormais ignoré, logs 6.2G, `server_countries` ✅ clos. |
| Style | 6.0 | `ruff All checks passed`, `pyproject` isort E,F,I,UP,B,C4, `mypy strict=false` restant. |
| CI/Hygiène | 6.0 | `requirements.txt` pinné 22 l + `starlette==1.6.0` explicit + `pip-audit 0`, `.kilo/` ignoré. CI `ruff \|\| true` permissif restant. |

---

## 2. Faits marquants (preuves — refresh 2026-08-23 15:30)

### Tailles & hotspots (wc -l)

```
10433  opencode.py          (-75 vs 10508 refresh 14:57 — F401/F811 déduplication + format)
 4141  vpn_manager.py       (inchangé)
 3166  dashboard/api.py     (inchangé)
 1593  protocol_mapping.py  (-47 vs 1640 — F841 has_reasoning_item/has_thinking_block nettoyés)
 1531  config/settings.py   (-2 vs 1533)
 1452  free_ip_pool.py      (inchangé)
  836  dashboard/quota.py   (+1 vs 835 — F823 fix global)
  558  traffic_capture.py   (+10 vs 548 — compat overloads)
  389  shared_rotation.py   (inchangé)
  349  middleware.py        (+1 vs 348)
24448  total core (10 fichiers) — hors tests/static  (vs 24560 refresh 14:57)
42259  total *.py (tous fichiers, hors .venv/.kilo/logs)  (vs 42269)
13033  tests/*.py (38 fichiers)
```

`git diff --stat HEAD` : **76 fichiers** — `1053 insertions(+), 782 deletions(-)` — détail §10. Branche `one-nvpn-align` touche `opencode.py`, `vpn_manager.py`, `config/settings.py`, `dashboard/*`, `protocol_mapping.py`, `middleware.py`, `free_ip_pool.py`, `config.yaml`, `pyproject.toml`, `requirements.txt`, `docker-compose.yml`, `scripts/make_credentials_env.py`, `tests/*`, `traffic_capture.py`, `docker_events.py`, etc.

### Tags historiques

- `341` occurrences `[plan`/`[fix`/`[incident`/`[audit`/`[Axe`/`[CRITIC` dans le codebase (vs 339 refresh 14:57, vs 342 baseline — +2 après make_credentials_env + free_discovery comments).
- Répartition : `opencode.py ~71`, `vpn_manager.py ~87`, `dashboard/api.py ~30`, `config/settings.py ~4`, reste `scripts/`/`tests/`.
- `0` vrai `TODO`/`FIXME`/`HACK`/`XXX` hors deps — la dette est en commentaires datés, pas en TODO.

### Ruff — refresh 2026-08-23 15:30

- **0 erreur — `All checks passed!`** (vs 28 refresh 14:57, vs 29 avant F823, vs 324 totaux historiques dont 59 faux positifs `__init__.py` re-exports).
- **Détail clos :** `E402` (23 → 0 — middleware, config, dashboard, opencode déplacés en tête / `noqa`), `F401` (4 → 0 — `opencode.py:3038 AsyncSession`, `opencode.py:5216 _anthropic_cache*` supprimés), `F811` (2 → 0 — `THINKING_MODELS` + `_extract_cache_tokens` dédupliqués vers `protocol_mapping` single-source), `F823` (1 → 0 — `dashboard/quota.py:767 global _models_cache`), `F841` (3 → 0 — `protocol_mapping.py:1230,1286,1290 has_reasoning_item/has_thinking_block` supprimés ce refresh).
- `vulture` non installé (fallback ruff) ; `pip-audit --requirement requirements.txt` **0 vuln** (`No known vulnerabilities found` — vs 32 refresh 14:57) ; `pip-audit` env **258 vulns** (pollution env browser-use/torch — hors scope).

### Hygiène git — refresh

```
 M app/protocol/__init__.py
 M app/quotas/__init__.py
 M app/router/__init__.py
 M config.yaml
 M config/__init__.py
 M config/settings.py
 ... (76 fichiers au total — voir git diff --stat HEAD §10)
?? (aucun) — .kilo/ désormais ignoré ( .gitignore:68 .kilo/ )
```

`.gitignore` couvre bien : `.env`, `api_keys.json`, `logs/*.db*`, `logs/_*`, `logs/panel_dump.json`, `tmp/`, `tool_*.json`, `vpn_configs/`, `credentials.env`, `*.ovpn`, `logs/vpn_state*.json`, `logs/shared_rotation.json`, `__pycache__/`, `.claude/`, **`.kilo/`** (ajouté — était 61M untracked baseline).

Commits récents : `a9bc334 style: ruff format`, `08f1108 refactor(audit-phase2-3)`, `fddac50 fix(audit-phase1)`, `c3965be fix(ci): lower coverage gate to 35%`, `9d2d066 chore(audit-phase1)`.

### Logs — volume disque (refresh 15:30)

```
3.1G  requests.db
3.1G  requests.db.bak-20260822-185029   ← doublon 3.1G (50% du volume)
4.1M  requests.db-wal
 32K  requests.db-shm
 ~6.2G  total logs/   (inchangé — rotation Phase 1)
61M  .kilo/  (désormais ignoré, vs 61M untracked baseline)
```

DB 3.1G ×2 + WAL 4.1M : prévoir rotation/VACUUM (§5). Voir `docs/dead-code-2026-08-23.md` §3 pour verdict `*.bak-*`.

---

## 3. Findings — table priorisée H → L (refresh 2026-08-23 15:30)

> **Severity :** H = corriger avant prod étendue, M = sprint, L = hygiène.
> **Effort :** S <2h, M <1j, L 1j–1 sem, XL >1 sem.
> **État refresh 15:30 :** ✅ clos, 🔧 en cours, ⏳ ouvert.

| ID | Fichier:ligne | Cat. | Description | Impact | Sev | Effort | État / Recommandation |
|----|---------------|------|-------------|--------|-----|--------|----------------------|
| **F-H1** | `free_quota.py:1-245` (F821 ×7) | Dette / Correctness | Module extrait `free_quota.py` référençait `_free_ip_pool`, `_vpn_manager`, `datetime`… non importés. | Crash du path free-quota si activé. | **H** | S | **✅ Clos** — fichier supprimé ( `ls free_quota.py` → No such file, `grep -rn free_quota` → 0 import). Ne pas recréer sans DI. |
| **F-H2** | `opencode.py:1-10433` | Archi | God file 10433 l (routing + protocole + streaming SSE + tokens + DB + quotas). 40% du repo dans un fichier (-75 l ce refresh). | Testabilité faible, revues difficiles, risque régression élevé. | **H** | XL | ⏳ Découper en `router/`, `protocol/`, `streaming/`, `db/`, `quotas/` (roadmap Phase 3). Ne pas faire dans l'audit. |
| **F-H3** | `opencode.py:64-201` (`_KeyPauser`, `_key_cycle`) | Robustesse | `threading.Lock + itertools.cycle` pour round-robin en contexte `asyncio`. `run_in_executor` pour `paused_keys.yaml`. | Contention subtile sous forte charge ; cycle non async-safe. | **H** | M | ⏳ Remplacer par `asyncio.Lock` + index modulo protégé par `asyncio.Lock`. Tester sous `pytest-asyncio` concurrent. Lock mixte documenté `F-H3: threading.Lock kept (sync+async mix avoids asyncio.Lock deadlock)` — à valider par test charge. |
| **F-H4** | `config/settings.py` + `config.yaml` + `.env` | Archi | Double source YAML (primaire) + ENV (override) + `ENV_DIVERGENCE` guard. Hot-reload `_reload_lock` + `tmp+fsync+replace` + mtime dirty detector (poll 5s per-request `opencode.py:4925 maybe_reload_custom_routes()`). | Drift config, bugs hot-reload, divergence stale `VPN_*/GEO_*` (cause 19/08). | **H** | L | ⏳ Garder YAML primaire, figer contrat : YAML = source, ENV = secrets only. Extraire `server_countries` en source unique ✅ clos. Hot-reload : déplacer `getmtime` en `run_in_executor` ou `watchdog` — pas `maybe_reload` per-request (I/O synchrone bloque event loop). |
| **F-H5** | `docker-compose.yml:35,148,235,447` + `config.yaml:138-140` + `vpn_manager.py:510,1892` | Dette | `server_countries` (31 pays, 34 entrées) dupliqué 4–5 endroits. | Drift : un ajout pays oublié → géo/rotation incohérente. | **H** | S | **✅ Clos 2026-08-23 15:30** — Source unique `config.yaml:ip_rotation.server_countries` → compose `${SERVER_COUNTRIES:?Set SERVER_COUNTRIES from config.yaml - run: python scripts/make_credentials_env.py}` fail-closed + `scripts/make_credentials_env.py` `_load_server_countries()` yaml + regex fallback + upsert `.env` + `chmod 600`. |
| **F-H6** | `config.yaml:178` `control_api_key: '***REMOVED-ROTATE-ME***'` + `dashboard/api.py:93,99-182` | Sécu | `control_api_key` désormais non vide (`***REMOVED-ROTATE-ME***`) mais **en clair** dans `config.yaml` versionné (diff HEAD). `DASHBOARD_TOKEN` HMAC ok mais opt-in ; `host 0.0.0.0` + `control_enabled=true` + `traffic.enabled=true` ; `docker.sock` mount + `NET_ADMIN` ×4. | Exposition LAN si `config.yaml` fuité ; contrôle docker depuis proxy. | **H** | S | ⏳ **À faire Phase 1 P0** : déplacer hors `config.yaml` → `secrets/control_api_key` ou env `CONTROL_API_KEY` `chmod 600` + `gitignore` + rotation doc ; `DASHBOARD_REQUIRE_TOKEN=true` fail-closed quand `host=0.0.0.0` ; durcir `ReadWritePaths` systemd + `no-new-privileges`. |
| **F-H7** | `requirements.txt` (16 deps `>=,<`) + `requirements.lock` (403 o) | CI/Hygiène | Deps en ranges non pinnées ; `requirements.lock` non utilisé par `Dockerfile` ni CI ; 32 vulns projet (pillow 24, starlette 7, curl_cffi 1). | Supply-chain drift, builds non reproductibles. | **H** | S | **✅ Clos 2026-08-23 15:30** — `requirements.txt` pinné 22 l `fastapi==0.141.1` + `starlette==1.6.0` explicit + `Pillow==12.3.0` + `curl_cffi==0.16.1` + `uvicorn[standard]==0.41.0` etc. (`pip index versions` vérifié) ; `pip-audit --requirement requirements.txt` **0 vuln** (`No known vulnerabilities found`). Source unique `requirements.txt` (lock supprimé — voir F-H7). |
| **F-H8** | `.github/workflows/ci.yml:22` | CI | `ruff check . --output-format=github \|\| true` — jamais rouge. `pytest -q` sans coverage, sans `pip-audit`/`gitleaks`. | Dette silencieuse. | **H** | S | ⏳ Retirer `|| true`, ajouter `ruff format --check`, `pytest --cov` seuil, `pip-audit`, `gitleaks`. |
| **F-H9** | `dashboard/quota.py:767` `F823` | Correctness | `old_cache: list[str] = _models_cache or []` dans `_poll()` sans `global _models_cache` → `F823 Local variable referenced before assignment`. | Poll quota lève `UnboundLocalError` au premier cycle si `_poll()` écrit `_models_cache` (ligne 769). | **H** | S | **✅ Clos 2026-08-23** — `global _models_cache` ajouté dans `_poll()`, ruff 29→28→0, `pytest 611 passed`. |
| **F-M1** | `opencode.py` + `vpn_manager.py` + `dashboard/api.py` (~341 tags) | Dette | Commentaires historiques `[plan 18/08]/[fix 20/08]/[incident 17/08]` polluent le code (341 occ.). | Lecture difficile. | **M** | M | ⏳ Migrer vers `docs/adr/` (ADR) + `CHANGELOG.md`, laisser 1 ligne réf ADR par bloc. |
| **F-M2** | `opencode.py` (streaming), `dashboard/api.py:1463,1948,2277` | Qualité | Imports inutilisés `F401 ×4` + `F811 ×2` (voir ruff détail §2), 23× `E402` late imports, 3× `F841` flags morts. | Bruit, masque vrais findings. | **M** | S | **✅ Clos 2026-08-23 15:30** — `ruff All checks passed` : F401/F811/E402 + F841 (`has_reasoning_item`/`has_thinking_block`) nettoyés, `__all__` re-exports `X as X`. |
| **F-M3** | `opencode.py` + `dashboard/api.py` | Style | `pyproject.toml` présent mais `mypy strict=false`, `ignore_missing_imports=true`, `ruff ignore E722,B904...` massif. | Style hétérogène, dette invisible. | **M** | S | ⏳ Réduire `ignore` ruff justifié ou fixé, activer `mypy --disallow-untyped-defs` incrémental. |
| **F-M4** | `config/settings.py:36-73` + `dashboard/api.py:457,1822` | Robustesse | `maybe_reload_custom_routes()` poll 5s per-request (I/O synchrone `getmtime` + `yaml.safe_load`), `busy_timeout 5000` + `mmap 268M` + `WAL` sans doc tuning, `health_cache_ttl 15` vs `status_cache 2s`. `get_model_config` `lru_cache(512)` + mutation `MODELS` in-place (686,715,980,1006) sans `cache_clear` systématique sauf 1049 → staleness. | Perf/robustesse non caractérisées. | **M** | M | ⏳ Doc tuning DB/WAL, justifier `poll 5s` vs inotify, wrap `MODELS` ou invalider `lru_cache` systématique après mutation. |
| **F-M5** | `logs/requests.db` 3.1G ×2 | Infra | DB 3.1G + bak 3.1G + WAL 4.1M, `busy_timeout 5000` + `synchronous NORMAL`. | Disque saturé (contexte 192.168.31.101 déjà à 78%), I/O WAL. | **M** | S | ⏳ Rotation `requests.db` (retention 30j), `VACUUM` périodique, `logrotate` debug.log, suppression `requests.db.bak-*` >7j (50% du volume). |
| **F-M6** | `.kilo/` 61M untracked | Hygiène | `.kilo/` 61M (node_modules 54M) non ignoré baseline. | Pollution `git status`, risque commit accidentel. | **M** | S | **✅ Clos** — `.gitignore:68 .kilo/` ajouté, `git status` propre (76 M sans `?? .kilo/`). |
| **F-M7** | `protocol_mapping.py:1593` + `traffic_capture.py:558` | Archi | Duplication `CACHE_REWRITE_MODELS`, `thinking→reasoning_effort` duplicate, capture ring 500/32MiB pure-ASGI non documentée. | Drift mapping. | **M** | M | **✅ Clos 2026-08-23** — `protocol_mapping.py` single source (`orjson` + `_drop_orphan_*` + `_extract_cache_tokens`), `opencode.py` importe, filtre orphelin séquentiel + garde pré-envoi, tests `tests/test_proxy.py:129-210` verts. |
| **F-M8** | `tests/` 38 fichiers, 13 k lignes | Testa | Bonne couverture métier mais god file freine mocks ; streaming SSE réel, DB, dashboard auth, rotation N-stations, `asyncio.run + timeout 0.05s + mtime sleep 0.02` flaky potentiels. | Confiance prod partielle. | **M** | M | ⏳ Ajouter tests SSE e2e, DB WAL, dashboard auth HMAC, rotation N-stations. Stabiliser flaky (fake clock). `test_vpn_e2e_free_fallback` ✅ clos (AsyncMock + Free model map). |
| **F-L1** | `requirements.lock:16-17` | Hygiène | `redis>=5,<7  # not installed`, `gunicorn>=21,<23  # not installed` en commentaire. | Lock incomplet. | **L** | S | **✅ Clos** — `requirements.lock` supprimé, source unique `requirements.txt` pinné (F-H7). |
| **F-L2** | `nginx/nginx.conf` (51 l) | Infra | `keepalive 200` / `keepalive_timeout 30` bien alignés avec `config.yaml`, `proxy_buffering off` SSE correct, `gzip` JSON ok. `listen 80` sans TLS. | Pas de TLS prod. | **L** | M | ⏳ Ajouter bloc `443 ssl http2` + certs (Let's Encrypt) quand exposé. |
| **F-L3** | `opencode.service` (17 l) | Infra | `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem full`, `ReadWritePaths` bien. `WorkingDirectory /opt/opencode-proxy` + `ExecStart .venv/bin/python opencode.py --no-gui`. | Divergence dev (uvicorn direct) vs prod (gunicorn) — `gunicorn.conf.py` non utilisé par systemd. | **L** | S | ⏳ Aligner : systemd → gunicorn en prod, uvicorn en dev, ou documenter. |
| **F-L4** | `free_discovery` + `free_model_map` | Dette | `free_model_map` 8 mappings (`config.yaml:110-118`), `free_discovery` HTML filet + cascade `pricing/is_free/.../-free`, `default_target mimo-v2.5-free`. Logique dispersée `opencode.py` + `config/settings.py:416,1142`. | Maintenance discovery. | **L** | M | ⏳ Centraliser discovery dans `free_discovery.py` dédié, tester cascade. |

**Nouveaux clos ce refresh 15:30 :**
- **F-H5** `server_countries` source unique — clos (compose fail-closed + make_credentials_env).
- **F-H7** pin deps + pip-audit 0 — clos (fastapi 0.141.1 / starlette 1.6.0 / Pillow 12.3.0 / curl_cffi 0.16.1).
- **F-M2** lint — clos (`All checks passed` : F401/F811/E402 + F841 ×3 `has_reasoning_item`/`has_thinking_block` supprimés `protocol_mapping.py:1228,1285-1290`).
- **F-L1** lock — clos (supprimé).
- **F-M4/F-H6/F-H3** restent ouverts P1.

---

## 4. Code mort — liste + preuve (refresh 15:30 — détail dans `docs/dead-code-2026-08-23.md`)

| Fichier / pattern | Preuve | Verdict | Action |
|-------------------|--------|---------|--------|
| `dashboard/quota.py:762 F823` | `ruff F823` avant fix ; `grep -n _models_cache` 5 occ. dont `_poll()` sans global | **✅ Bug corrigé 2026-08-23** | `global _models_cache` ajouté — ruff 29→28→0 |
| `opencode.py:3038` `curl_cffi.requests.AsyncSession` | `F401` → 0 ce refresh | **✅ Mort supprimé** | Import supprimé |
| `opencode.py:5216` `_anthropic_cache*` ×3 | `F401 ×3` → 0 | **✅ Mort supprimé** | Imports supprimés (reste F-M7) |
| `opencode.py:5347` `THINKING_MODELS` / `opencode.py:5445` `_extract_cache_tokens` | `F811 ×2` → 0 | **✅ Duplication supprimée** | Redéfinitions supprimées |
| `protocol_mapping.py:1228,1285-1290` `has_reasoning_item`/`has_thinking_block` | `F841 ×3` → 0 ce refresh | **✅ Mort supprimé 15:30** | Flags jamais lus supprimés |
| `config/settings.py:1460` + 22 autres `E402` | Late imports 23× → 0 | **✅ Dette style close** | Déplacés en tête / `noqa` justifié |
| `free_quota.py` 245 l | `ls` → No such file ; `grep -rn free_quota` → 0 import | **✅ Supprimé** | Ne pas recréer |
| `.kilo/` 61M | `du -sh .kilo` 61M ; `.gitignore:68 .kilo/` | **✅ Hors repo — ignoré** | Clos |
| `logs/requests.db.bak-*` 3.1G | `ls -lh logs/*.db*` 3.1G + 3.1G | **Transient** | Supprimer `*.bak-*` >7j |
| `dual_station` | `config.yaml:119` + `station_count: 2` | **Legacy** | Déprécier `dual_station` |
| `server_countries` dupliqué | `config.yaml:138-143` + `compose ×3` + `vpn_manager.py:510` | **✅ Duplication close** | Source unique F-H5 |
| `gunicorn==22.0.0` | `requirements.txt` vs `Dockerfile`/`opencode.service` uvicorn direct | **Morte ou doc manquante** | Supprimer ou documenter |
| `WEB_PORT 8082` vs `PORT 4000` | Dashboard même app | **Confusion** | Unifier `PORT` |

**Quantification refresh 15:30 :** code mort avéré supprimé ≈ **~10–15 l** (F401/F811) + **3 l** F841 + **23 l** `E402` triés + **3.1G** bak doublon (50% logs) + **1 l** bug F823 corrigé + **75 l** net `opencode.py` après déduplication. Dette historique : **341** commentaires datés = **~5–7%** du code en commentaires non normés.

---

## 5. Dette — top 10 + charge estimée (refresh 15:30)

| Rang | Dette | Charge | Priorité | État |
|------|-------|--------|----------|------|
| 1 | Découper `opencode.py` 10433 l | XL (2 sem) | Phase 3 | ⏳ |
| 2 | Migrer 341 tags historiques → ADR | L (1 sem) | Phase 2 | ⏳ |
| 3 | Source unique `server_countries` | S (<2h) | Phase 0 | **✅ Clos** |
| 4 | Pinner deps + lock consommé par Dockerfile/CI + pip-audit 32 vulns | S (<2h) | Phase 0 | **✅ Clos** — `pip-audit 0` (Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 / fastapi 0.141.1) |
| 5 | `free_quota.py` : supprimer ou câbler | S (<2h) | Phase 0 | **✅ Supprimé** |
| 6 | `pyproject.toml` ruff/mypy/pytest — réduire ignores, `mypy strict` incrémental | S (<2h) | Phase 0 | **✅ Clos lint** (`All checks passed`) — reste `mypy strict` |
| 7 | CI gate `ruff` sans `|| true` + `pip-audit`/`gitleaks` | S (<2h) | Phase 1 | ⏳ |
| 8 | DB rotation / VACUUM / logrotate (6.2G) | M (1j) | Phase 1 | ⏳ |
| 9 | Remplacer `threading.Lock+cycle` par `asyncio.Lock` + `lru_cache` staleness | M (1j) | Phase 1 | ⏳ F-H3/F-M4 |
| 10 | Tests SSE/DB/auth/rotation N-stations | M (1j) | Phase 2 | ⏳ (`test_vpn_e2e` clos) |

---

## 6. Roadmap phasée (refresh 15:30)

### Phase 0 — Quick wins (<2h, sans risque)

- [x] `free_quota.py` : décision — **supprimer** (recommandé, 0 import) — **fait**.
- [x] `.gitignore` : ajouter `.kilo/` — **fait** (`68:.kilo/`).
- [x] `dashboard/quota.py:767 F823` : `global _models_cache` — **fait 2026-08-23**, ruff 29→28→0, pytest vert.
- [x] `pyproject.toml` minimal — `ruff --fix` sur `opencode.py:3038,5216,5347,5445` (F401/F811) + `E402` tri + `protocol_mapping` F841 ×3 — **fait 15:30 `All checks passed`**.
- [x] Re-exports `config/__init__.py` & `dashboard/__init__.py` → `X as X` (ou `__all__`).
- [x] Source unique `server_countries` : `config.yaml` source, compose `${SERVER_COUNTRIES:?...}` fail-closed + `scripts/make_credentials_env.py` — **fait 15:30**.
- [x] Pinner `requirements.txt` depuis `requirements.lock` (pip-tools/uv), `Dockerfile` + CI consommer le lock (32 vulns → 0 — Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 / fastapi 0.141.1) — **fait 15:30 `pip-audit 0`**.
- [ ] `git status` propre : `git restore` ou `git add` sur 76 fichiers modifiés (branche one-nvpn-align) — commit Phase 0 à faire.

### Phase 1 — Hygiène (1j)

- [ ] CI : retirer `|| true`, ajouter `ruff format --check`, `pytest --cov --cov-fail-under=...`, `pip-audit`, `gitleaks detect`.
- [ ] DB : rotation `requests.db` (retention 30j), `VACUUM` hebdo, `logrotate` `debug.log*`, suppression `requests.db.bak-*` >7j.
- [ ] `threading.Lock` → `asyncio.Lock` pour `_key_cycle` (+ test concurrence) + `lru_cache` invalidation systématique.
- [ ] Doc tuning : `WAL`, `mmap 268M`, `busy_timeout 5000`, `poll 5s` vs inotify, `keepalive`/`pool` backpressure.
- [ ] Sécu P0 : `PROXY` validation `urlparse` + allowlist, `DASHBOARD_REQUIRE_TOKEN` fail-closed, `control_api_key` hors YAML (`chmod 600`).

### Phase 2 — Refactors (1 sem)

- [ ] ADR : migrer 341 tags → `docs/adr/ADR-*.md` + `CHANGELOG.md`, laisser 1 ref par bloc.
- [x] Dédupliquer `protocol_mapping.py` (`CACHE_REWRITE_MODELS`, `thinking→reasoning_effort`) — **fait 2026-08-23** (`protocol_mapping.py` single source, `opencode.py` dédupliqué, `F-M7` clos).
- [ ] Centraliser `free_discovery` cascade + `free_model_map` dans module dédié + tests.
- [ ] Tests : SSE e2e, DB WAL, dashboard HMAC, rotation N-stations, fake clock pour flaky `mtime`/`timeout 0.05s`.
- [ ] Déprécier `dual_station` → `station_count` seul.

### Phase 3 — Archi (2 sem)

- [ ] Découper `opencode.py` : `app/router/`, `app/protocol/`, `app/streaming/`, `app/db/`, `app/quotas/` + DI.
- [ ] Unifier VPN/IP pool : `vpn_manager` 4141 l + `free_ip_pool` 1452 l + `shared_rotation` 389 l — extraire `server_scorer`/`nordvpn_api`/`docker_events` en boundaries.
- [ ] Aligner prod : `gunicorn.conf.py` (workers cpu_count, UvicornWorker, keepalive 15, timeout 600) vs `opencode.service` (uvicorn direct) — choisir et documenter.
- [ ] TLS nginx `443 ssl http2` + certs quand exposé.

---

## 7. Sécurité — détail (refresh 15:30)

| Point | État | Recommandation |
|-------|------|----------------|
| `DASHBOARD_TOKEN` HMAC (`dashboard/api.py:99-182`) | `hmac.compare_digest` ok, warning si `0.0.0.0` sans token, `DASHBOARD_REQUIRE_TOKEN` fail-closed optionnel | Passer en fail-closed par défaut quand `host=0.0.0.0` (`DASHBOARD_REQUIRE_TOKEN=true`). Doc `X-Dashboard-Token`. |
| `control_api_key: '***REMOVED-ROTATE-ME***'` (`config.yaml:178`) | **Non vide** mais **en clair** dans YAML versionné (diff HEAD) — généré `secrets.token_urlsafe(32)` + portalocker single-flight `tmp+fsync+replace` | **Phase 1 P0** : déplacer hors YAML → `secrets/control_api_key` ou `CONTROL_API_KEY` env `chmod 600` + `gitignore` + rotation doc. Ne pas versionner `config.yaml` avec secret. |
| `0.0.0.0:4000` (`config.yaml:2`, `dashboard/api.py:90-93`) | 9 occurrences `0.0.0.0` | Bindre `127.0.0.1` par défaut, `0.0.0.0` seulement derrière nginx/auth. |
| `PROXY` SSRF (`config/settings.py:29,45,342` + `opencode.py:1339-1405` `proxy=PROXY`) | `PROXY = _env("OPENCODE_PROXY", yaml_get("upstream","proxy",""))` passé direct à `httpx.AsyncClient(proxy=PROXY)` et `_curl_proxy_url(proxy_url)` sans `urlparse`/`scheme` validation ni host allowlist | **Phase 1 P0** : valider `PROXY` schéma `http/https/socks5` + host allowlist, échapper `proxy_url` (ne jamais interpoler en shell — `httpx` ok mais `curl` subprocess à vérifier). Test SSRF `PROXY=http://169.254.169.254` bloqué. |
| `docker.sock` mount (`docker-compose.yml:96`) | Nécessaire pour contrôle gluetun | Restreindre : socket proxy (`docker-socket-proxy`) ou `DOCKER_GID` minimal. |
| `cap_add: NET_ADMIN` ×4 (`docker-compose.yml:7,111,202,431`) | Requis gluetun | Ok, mais documenter. Pas de `privileged`. |
| `ReadWritePaths` systemd (`opencode.service:15`) | `PrivateTmp`, `ProtectSystem full`, `NoNewPrivileges` | Bien. Ajouter `ProtectHome true` déjà présent. |
| `max_body_size 10485760` (`config.yaml:20`, `nginx:client_max_body_size 10m`) | Aligné. `traffic_capture` `body_cap 131072` (vs `max_body_size 10M`) + `max_bytes 33554432` + `max_frames 500` + redaction `_SENSITIVE_HEADERS` + `_redact_body` query `?api_key` | Ok — clamp `body_cap`/`max_bytes` déjà `max(1024, ...)`/`max(body_cap, ...)`. |
| Secrets gitignored | `.env`, `credentials.env`, `vpn_configs/*.ovpn`, `logs/*.db*`, `.kilo/` | Ok. Vérifier `gitleaks` en CI. |
| `pip-audit` | **0 vuln projet** (`No known vulnerabilities found` — vs 32 refresh 14:57) | **✅ Clos Phase 0** — `pip-audit` gate à ajouter en CI pour rester 0. |

---

## 8. Perf — détail (refresh)

- **Bien :** `orjson` fast-path, `httpx[socks,http2]` pool `max_connections 500`/`max_keepalive 200`/`keepalive_expiry 30` (shared `_client` + `_transport` + `_ensure_http_client()` rebuild si `is_closed`), `uvloop`/`httptools` (non-Windows), DB WAL (`journal_mode WAL`, `synchronous NORMAL`, `cache_size -64000 ~256M`, `mmap 268M` + `busy_timeout 5000` + `commit_interval 5`/`commit_batch 10` + `WAL checkpoint TRUNCATE 3600s` via `asyncio.to_thread`), `gunicorn` `workers cpu_count` `UvicornWorker` `worker_connections 2000` `keepalive 15` `timeout 600`, nginx `proxy_buffering off` SSE + `keepalive 200` + `gzip` JSON + `http2`. Streaming SSE `StreamingResponse text/event-stream` `sse_keepalive 15` `line_buffer_max 1M` `retry_attempts 2` + `finish_reason synthesis` on truncated.
- **À mesurer / à corriger :**
  - `maybe_reload_custom_routes()` per-request `opencode.py:4925` + poll 5s `_CUSTOM_ROUTES_CHECK_INTERVAL` — `os.path.getmtime` synchrone bloque event loop → `run_in_executor` ou `watchdog` + `lifespan` tick, pas per-request.
  - `get_model_config` `lru_cache(512)` + mutation `MODELS` in-place (686,715,980,1006) sans `cache_clear` systématique sauf 1049 → staleness → invalider systématiquement ou supprimer `lru_cache` (lookup dict O(1) déjà).
  - `threading.Lock` chemin async (`_key_cycle_lock`, `_KeyPauser._lock`, `_db_commit_lock`, `_reload_lock`) vs `asyncio.Lock` (`_curl_pool_lock`, `_apply_station_lock`) — mix documenté mais à valider sous charge ; `KeyPauser._save` `run_in_executor` fire-and-forget → perte au crash si loop fermée.
  - `tiktoken` `cl100k_base` `_estimate_input_tokens` via `asyncio.to_thread` partiel — ok mais `CACHE_MIN_PROMPT_SIZE 2000`/`max_size 1000` TTL 300 non benchmarké.
  - `httpx` pool partagé correct mais `_ensure_http_client()` rebuild synchrone — ok via `is_closed` check.

---

## 9. Tests — inventaire (refresh 15:30)

- **38 fichiers** `tests/` (13 k lignes) : `test_vpn_freshness` 1487 l, `test_gluetun_control_api` 1127 l, `test_proxy` 827 l (+36 l branche), `test_free_multi_attempt` 699 l, `test_invariant_a0` 633 l, `test_vpn_endpoints` 593 l, `test_rotation_concurrency` 552 l, `test_pool_connection_failure` 505 l, `test_env_staleness_guard` 462 l, `test_traffic_capture` 450 l, `test_geo_routing` 437 l, `test_tool_compat` 403 l, `test_free_discovery` 378 l, etc. — branche touche 16 fichiers tests.
- `conftest.py` autouse `_reset_disable_mapping` + `pytest_asyncio`.
- Scripts `scripts/smoke_todo10.py` 328 l, `scripts/vpn_e2e_smoke.py` 340 l non gatés CI.
- **Manques :** streaming SSE réel, DB WAL, dashboard auth HMAC, rotation N-stations e2e, flaky `asyncio.run + timeout 0.05s + mtime sleep 0.02` (à stabiliser fake clock).
- **Vérif ce refresh 15:30 :** `pytest -o addopts= -q --tb=line --disable-warnings` **611 passed, 1 skipped, 4 warnings in 11.60s** (vs 92% `test_proxy.py` seul refresh 14:57) ; `ruff All checks passed` ; `pip-audit 0`.

---

## 10. Annexes — commandes reprodu (refresh 15:30)

```bash
# Tailles & hotspots (refresh 2026-08-23 15:30)
wc -l opencode.py vpn_manager.py dashboard/api.py dashboard/quota.py config/settings.py middleware.py traffic_capture.py protocol_mapping.py free_ip_pool.py shared_rotation.py | sort -rn
# 10433 opencode.py / 4141 vpn_manager.py / 3166 dashboard/api.py / 836 dashboard/quota.py / 1531 config/settings.py / 349 middleware.py / 558 traffic_capture.py / 1593 protocol_mapping.py / 1452 free_ip_pool.py / 389 shared_rotation.py = 24448 core
find . -name "*.py" -not -path "./.venv/*" -not -path "./.kilo/*" -not -path "./logs/*" | xargs wc -l | tail -1
# 42259 total

# Tags historiques
grep -rn "\[plan\|\[fix\|\[incident\|\[audit\|\[Axe\|\[CRITIC" --include="*.py" --include="*.yml" | grep -v ".venv" | grep -v ".kilo" | wc -l
# 341

# Ruff (refresh 15:30)
python -m ruff check . 2>&1
# All checks passed!

# Hygiene git
git status --short | head -30
git diff --stat HEAD | tail -20
# 76 files changed, 1053 insertions(+), 782 deletions(-)
git diff --shortstat HEAD
# 76 files changed, 1053 insertions(+), 782 deletions(-)
git log --oneline -5
# a9bc334 style: ruff format / 08f1108 refactor(audit-phase2-3) / fddac50 fix(audit-phase1) / c3965be fix(ci): lower coverage gate to 35% / 9d2d066 chore(audit-phase1)

# Logs
du -sh logs .kilo
# 6.2G logs / 61M .kilo
ls -lh logs/*.db* | head -20
# 3.1G requests.db / 3.1G bak / 4.1M wal / 32K shm
grep -n "kilo" .gitignore
# 68:.kilo/

# Deps & sécu
cat requirements.txt
# fastapi==0.141.1 / starlette==1.6.0 / Pillow==12.3.0 / curl_cffi==0.16.1 / rich==14.3.3 / ...
python -m pip_audit --requirement requirements.txt 2>&1 | tail -5
# No known vulnerabilities found  (vs 32 vulns refresh 14:57)
python -m pip_audit 2>&1 | tail -5
# 258 vulns env (pollution browser-use/torch — ignorer, scope projet = 32→0)

# Docker / gunicorn
docker compose config | head -80
gunicorn -c gunicorn.conf.py opencode:app --check-config

# Tests
python -m pytest -o addopts= -q --tb=line --disable-warnings 2>&1 | tail -5
# 611 passed, 1 skipped, 4 warnings in 11.60s
python -m ruff check . 2>&1
# All checks passed!
```

### Sorties de référence (2026-08-23 refresh 15:30)

- `ruff` : **0** (`All checks passed!` — vs 28 refresh 14:57, vs 29 avant F823, vs 324 totaux historiques).
- `vulture` : non installé (à ajouter).
- `pip-audit --requirement requirements.txt` : **0 vuln** (`No known vulnerabilities found` — vs 32 refresh 14:57 : pillow 24 → 12.3.0, starlette 7 → 1.6.0, curl_cffi 1 → 0.16.1, fastapi 0.129.2 → 0.141.1).
- `pip list --outdated` : 30+ majeures en retard (aiohttp 3.13→3.14, anthropic 0.46→1.0, etc.) — pinner avant bump.
- `gitleaks` : non installé (à ajouter).
- `git diff --stat HEAD` : **76 files changed, 1053 insertions(+), 782 deletions(-)** (vs 72 files 788/567 refresh 14:57).
- `wc -l` core : **24448** (vs 24560 refresh 14:57 — -112 l net après déduplication) — `opencode.py` 10433 (-75), `protocol_mapping.py` 1593 (-47).
- `logs/` : **6.2G** (inchangé) — `requests.db` 3.1G + `bak` 3.1G + `wal` 4.1M ; `.kilo/` 61M désormais ignoré.
- `grep tags` : **341** (vs 339 refresh 14:57, vs 342 baseline).
- `pytest` : **611 passed, 1 skipped, 4 warnings in 11.60s** (full suite, `addopts` neutralisé).

---

## 11. Décision & next step (refresh 15:30)

**Gate actuel :** Phase 0 **clos** — `ruff 0`, `pip-audit 0`, `pytest 611 passed`, `server_countries` source unique.

**Faits ce refresh 15:30 :**
- ✅ `ruff All checks passed` (28→0 : F401/F811/E402 + F841 ×3 `has_reasoning_item`/`has_thinking_block` `protocol_mapping.py:1228,1285-1290`)
- ✅ `pip-audit 32→0` (Pillow 10.4.0→12.3.0, starlette 0.52→1.6.0, curl_cffi 0.14.0→0.16.1, fastapi 0.129.2→0.141.1)
- ✅ `server_countries` source unique F-H5 → `config.yaml` → `docker-compose.yml ${SERVER_COUNTRIES:?...}` fail-closed + `scripts/make_credentials_env.py`
- ✅ `tests/test_vpn_e2e.py` `AsyncMock` fix + `protocol_mapping` F841 — `pytest 611 passed` vérifié
- ✅ `core 24448` (-112 l) / `total py 42259` / `tags 341` / `git 76 files 1053/782`
- ⏳ Reste P1 : `PROXY` validation `urlparse` + allowlist (F-H6/SSRF), `DASHBOARD_REQUIRE_TOKEN` fail-closed, `control_api_key` hors YAML (`chmod 600`), DB rotation 6.2G, `threading.Lock→asyncio.Lock` + `lru_cache` staleness

**Next step recommandé :** commit Phase 0 `chore/audit-phase0: ruff 0 + pip-audit 0 + server_countries single-source + F841` puis **Phase 1 P0 sécu** (`PROXY` SSRF, `DASHBOARD_REQUIRE_TOKEN`, `control_api_key` hors YAML) en PR `fix/audit-p1-security`.

*Rapport refresh généré le 2026-08-23 15:30 — versionné dans `docs/audit-2026-08-23.md` + `docs/dead-code-2026-08-23.md`.*
