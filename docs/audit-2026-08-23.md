# Audit opencode-proxy — 2026-08-23

> **Périmètre :** `opencode-proxy` — proxy FastAPI Claude Code → opencode.ai
> **Branche auditée :** `one-nvpn-align` (base `main`), commit `b56b00a` + diff local `config.yaml` / `opencode.py` non commité.
> **Méthode :** 4 passes — A/ scans automatiques (tailles, grep, ruff, hygiene git), B/ revue 9 dimensions H/M/L, C/ quantification dette & code mort, D/ rapport & roadmap. Outillage : `ruff`, `vulture` (non installé, fallback ruff F401/F841), `grep`, `wc -l`, `git`, `pip list --outdated`.
> **Auteur :** audit automatisé (contexte ultracode) — à relire/contresigner humain.

---

## 1. Score global & radar

### Score global : **6,2 / 10** — *Correct, exploitable en prod avec dette ciblée à traiter*

Le projet est **fonctionnellement solide** (perf, infra Docker/nginx/gunicorn soignées, copie explicite, healthcheck, rate-limit, SSE, quotas, rotation multi-station) mais porte une **dette structurelle** (god file 8,8 kl, 342 commentaires historiques, config double source, deps non pinnées, module cassé `free_quota.py`, CI permissive). Aucun blocage prod immédiat hors `free_quota.py` à l'import indirect.

### Radar 9 axes (0 = critique, 10 = exemplaire)

```
Architecture & couplage  ██████░░░░  5.5
Dette & code mort        █████░░░░░  5.0
Qualité & style          █████░░░░░  5.0
Antipatterns/robustesse  ██████░░░░  6.0
Testabilité              ██████░░░░  6.5
Sécurité                 ██████░░░░  6.0
Performance              ████████░░  8.0
Infra & déploiement      ███████░░░  7.5
CI / Hygiène & deps      ████░░░░░░  4.5
```

| Axe | Note | Raison courte |
|-----|------|---------------|
| Perf | 8.0 | orjson, httpx http2/socks, uvloop/httptools, gunicorn cpu_count, nginx SSE-safe — bien. Tuning pool non mesuré. |
| Infra | 7.5 | Dockerfile pinned, explicit COPY --chown, HEALTHCHECK, nginx buffers complets. Compose 482 l complexe. |
| Testabilité | 6.5 | 38 fichiers tests, ~584 `def test_*` (13 k lignes cumulées), fixtures conftest. God file freine la couverture unitaire. |
| Sécu | 6.0 | DASHBOARD_TOKEN HMAC + warning 0.0.0.0, secrets gitignored, COPY explicite. Mais `control_api_key=''` vide + docker.sock mount. |
| Robustesse | 6.0 | RateLimit/CircuitBreaker pure-ASGI, WAL SQLite sérieux, retry. Mais `threading.Lock + cycle` en async, poll 5s, mmaps 268M sans doc WAL. |
| Archi | 5.5 | God file 8767 l + 3903 l + 2861 l concentrent 78% du code. Couplage config facade 40+ symboles. |
| Dette | 5.0 | 342 tags historiques, free_quota dead, .kilo 61M untracked, logs 6.4 GB, server_countries dupliqué. |
| Style | 5.0 | Pas de pyproject/ruff/mypy, style hétérogène, print vs logger, OrderedDict legacy. |
| CI/Hygiène | 4.5 | `requirements.txt` en ranges, `requirements.lock` inutilisé, CI `ruff || true` jamais rouge, pas de pip-audit/gitleaks gate. |

---

## 2. Faits marquants (preuves)

### Tailles & hotspots

```
8767  opencode.py
3903  vpn_manager.py
2861  dashboard/api.py
1410  free_ip_pool.py
1254  config/settings.py
1182  protocol_mapping.py
 493  traffic_capture.py
 378  shared_rotation.py
 286  middleware.py
 268  nordvpn_api.py
 245  free_quota.py          ← cassé (voir §3)
 240  server_scorer.py
 193  docker_events.py
  21  shared_state.py
21501 total (hors tests/static)
13033 tests/*.py (38 fichiers)
```

### Tags historiques

- `342` occurrences `[plan`/`[fix`/`[incident`/`[audit`/`[Axe`/`[CRITIC` dans le codebase.
- Répartition : `opencode.py ~71`, `vpn_manager.py ~87`, `dashboard/api.py ~30`, `config/settings.py ~4`, `free_quota.py ~1`, `scripts/` ~rest.
- `0` vrai `TODO`/`FIXME`/`HACK`/`XXX` hors deps (recherche `grep -rn` vide sur `*.py` métier) — la dette n'est pas en TODO mais en commentaires datés.

### Ruff (F401/F841/F821)

- `324` diagnostics totaux ; `~59` sont des faux positifs `__init__.py` re-exports sans `as` (style, pas bug — corrigeable par `X as X`).
- **Vrais findings hors `__init__.py` : 9 F401 + 3 F841 + 7 F821 = 19.**
- `vulture` non installé (fallback ruff) ; `pip-audit`/`gitleaks` non installés — à gater en CI.

### Hygiène git

```
 M config.yaml
 M opencode.py
?? .kilo/                 ← 61M, non ignoré ( .gitignore couvre .claude/ mais pas .kilo/ )
?? .kilo/plans/1787403253678-audit-complet-plan-correctif.md
 2 files changed, 117 insertions(+), 25 deletions(-)  (git diff --stat HEAD)
```

`.gitignore` couvre bien : `.env`, `api_keys.json`, `logs/*.db*`, `logs/_*`, `logs/panel_dump.json`, `tmp/`, `tool_*.json`, `vpn_configs/`, `credentials.env`, `*.ovpn`, `logs/vpn_state*.json`, `logs/shared_rotation.json`, `__pycache__/`, `.claude/`.

### Logs — volume disque

```
 775K  _400_report.txt
 4.2M  debug.log
10.5M  debug.log.1
 3.3G  requests.db
 3.3G  requests.db.bak-20260822-185029   ← doublon 3.3G
 4.3M  requests.db-wal
  32K  requests.db-shm
 ~6.4G  total logs/
```

DB 3.28G ×2 + WAL 4.3M : prévoir rotation/VACUUM ( §5 ).

---

## 3. Findings — table priorisée H → L

> **Severity :** H = corriger avant prod étendue, M = sprint, L = hygiène.
> **Effort :** S <2h, M <1j, L 1j–1 sem, XL >1 sem.

| ID | Fichier:ligne | Cat. | Description | Impact | Sev | Effort | Recommandation |
|----|---------------|------|-------------|--------|-----|--------|----------------|
| **F-H1** | `free_quota.py:1-245` (F821 ×7) | Dette / Correctness | Module extrait `free_quota.py` référence `_free_ip_pool`, `_vpn_manager`, `datetime`, `_debug`, `_log`, `_current_free_attempt` non importés/définis. Importé indirectement → `NameError` à l'exécution du path free-quota. | Crash du path free-quota si activé ; masqué si non importé mais dette bloquante. | **H** | S | Soit supprimer le fichier (s'il est dead extrait P3.10), soit le câbler : imports + injection dépendances. Vérifier `grep -rn free_quota` — 0 import trouvé → **suppression recommandée** ou finaliser l'extraction. |
| **F-H2** | `opencode.py:1-8767` | Archi | God file 8767 l (routing + protocole + streaming SSE + tokens + DB + quotas). 40% du repo dans un fichier. | Testabilité faible, revues difficiles, risque régression élevé. | **H** | XL | Découper en `router/`, `protocol/`, `streaming/`, `db/`, `quotas/` (roadmap Phase 2). Ne pas faire dans l'audit. |
| **F-H3** | `opencode.py:44-120` (`_KeyPauser`, `_key_cycle`) | Robustesse | `threading.Lock + itertools.cycle` pour round-robin en contexte `asyncio`. `run_in_executor` pour `paused_keys.yaml`. | Contention/concurrence subtile sous forte charge ; cycle non async-safe. | **H** | M | Remplacer par `asyncio.Lock` + index modulo ou `itertools.cycle` protégé par `asyncio.Lock`. Tester sous `pytest-asyncio` concurrent. |
| **F-H4** | `config/settings.py` + `config.yaml` + `.env` | Archi | Double source YAML (primaire) + ENV (override) + `ENV_DIVERGENCE` guard. Hot-reload coalescé `_reload_lock` + `tmp+fsync+replace` + mtime dirty detector. Complexe, duplication `server_countries`. | Drift config, bugs hot-reload, divergence stale `VPN_*/GEO_*` (cause 19/08). | **H** | L | Garder YAML primaire, figer contrat : YAML = source, ENV = secrets only. Extraire `server_countries` en source unique (Phase 1). Documenter hot-reload. |
| **F-H5** | `docker-compose.yml:35,148,235,447` + `config.yaml:138-140` + `vpn_manager.py:510,1892` | Dette | `server_countries` (31 pays, 34 entrées) dupliqué 4–5 endroits (compose ×3 + yaml + vpn_manager default). Compose 482 l, 10 gluetuns. | Drift : un ajout pays oublié quelque part → géo/rotation incohérente. | **H** | S | Source unique : `config.yaml:ip_rotation.server_countries` → compose via `env_file`/`${SERVER_COUNTRIES}` ou génération. Supprimer littéraux dupliqués. |
| **F-H6** | `config.yaml:175` `control_api_key: ''` + `dashboard/api.py:93,999` | Sécu | `control_api_key` vide par défaut ; `DASHBOARD_TOKEN` vide + `host 0.0.0.0` → warning mais pas de blocage. `docker.sock` mount + `NET_ADMIN` + `cap_add` sur 4 services. | Exposition LAN du dashboard sans auth ; contrôle docker depuis le proxy. | **H** | S | Exiger `DASHBOARD_TOKEN` quand `host=0.0.0.0` (fail-closed optionnel) ; générer `control_api_key` au premier boot ; durcir `ReadWritePaths` systemd + `no-new-privileges`. |
| **F-H7** | `requirements.txt` (16 deps `>=,<`) + `requirements.lock` (403 o) | CI/Hygiène | Deps en ranges non pinnées ; `requirements.lock` du 22/08 non utilisé par `Dockerfile` ni CI ; `redis`/`gunicorn` non installés localement mais attendus. | Supply-chain drift, builds non reproductibles. | **H** | S | Pinner `requirements.txt` via `pip-tools`/`uv` à partir du lock, faire consommer le lock par `Dockerfile` + CI. Ajouter `pip-audit` gate. |
| **F-H8** | `.github/workflows/ci.yml:22` | CI | `ruff check . --output-format=github \|\| true` — jamais rouge. `pytest -q` sans coverage, sans `pip-audit`/`gitleaks`. | Dette silencieuse, régressions non bloquées. | **H** | S | Retirer `|| true`, ajouter `ruff format --check`, `pytest --cov` seuil, `pip-audit`, `gitleaks`. |
| **F-M1** | `opencode.py` + `vpn_manager.py` + `dashboard/api.py` (~342 tags) | Dette | Commentaires historiques `[plan 18/08]/[fix 20/08]/[incident 17/08]/[Axe 3.1]` polluent le code (342 occ.). | Lecture difficile, archéo vs code. | **M** | M | Migrer vers `docs/adr/` (ADR) + `CHANGELOG.md`, laisser 1 ligne réf ADR par bloc. |
| **F-M2** | `opencode.py` (streaming), `dashboard/api.py:1463,1948,2277` | Qualité | Imports inutilisés (`tempfile` dans `config/settings.py:10`, `CONFIG_KEYS`/`CUSTOM_ROUTES`/`config.settings`/`os`/`json` dans `dashboard/api.py`), 3 `F841` locals morts (`status`, `group`, `root`), 1 `F821` `_debug` dans `display.py:342`. | Bruit, masque vrais findings. | **M** | S | `ruff --fix` + `__all__` re-exports `X as X`. Corriger `display.py:342` (`_debug` → `log`/`debug`). |
| **F-M3** | `opencode.py` + `dashboard/api.py` | Style | Pas de `pyproject.toml`/`ruff.toml`/`mypy.ini`, pas de formatter lock, `OrderedDict` legacy, `_CREATE_NO_WINDOW` win32 branch dispersé. | Style hétérogène, pas de gate format. | **M** | S | Ajouter `pyproject.toml` (ruff, mypy, pytest, coverage). |
| **F-M4** | `config/settings.py:36-73` + `dashboard/api.py:457,1822` | Robustesse | `maybe_reload_custom_routes()` poll 5s, `busy_timeout 5000` + `mmap 268M` + `WAL` sans doc tuning, `health_cache_ttl 15` vs `status_cache 2s`. | Perf/robustesse non caractérisées. | **M** | M | Doc tuning DB/WAL, mesurer `cache_size 64000` vs `mmap 268M`, justifier `poll 5s` vs inotify. |
| **F-M5** | `logs/requests.db` 3.28G ×2 | Infra | DB 3.28G + bak 3.28G + WAL 4.3M, `debug.log` 14.7M cumulé, pas de rotation/VACUUM. `busy_timeout 5000` + `synchronous NORMAL`. | Disque saturé (contexte 192.168.31.101 déjà à 78%), I/O WAL. | **M** | S | Rotation `requests.db` (retention 30j), `VACUUM` périodique, `logrotate` debug.log, supprimer `*.bak-*` ancien. |
| **F-M6** | `.kilo/` 61M untracked | Hygiène | `.kilo/` 61M (node_modules 54M) non ignoré, `?? .kilo/plans/...md`. `.gitignore` couvre `.claude/` mais pas `.kilo/`. | Pollution `git status`, risque commit accidentel. | **M** | S | Ajouter `.kilo/` à `.gitignore` ou déplacer hors repo. |
| **F-M7** | `protocol_mapping.py:1182` + `traffic_capture.py:493` | Archi | Duplication `CACHE_REWRITE_MODELS` (L17/L50), `thinking→reasoning_effort` duplicate L332 vs L733, capture ring 500/32MiB pure-ASGI non documentée. | Drift mapping, perf capture non mesurée. | **M** | M | Dédupliquer constantes, extraire `protocol_mapping` tests unitaires, doc `traffic_capture` budget. |
| **F-M8** | `tests/` 38 fichiers, 13 k lignes | Testa | Bonne couverture métier mais god file freine mocks ; streaming SSE réel, DB, dashboard auth, rotation N-stations, `asyncio.run + timeout 0.05s + mtime sleep 0.02` flaky potentiels. | Confiance prod partielle. | **M** | M | Ajouter tests SSE e2e, DB WAL, dashboard auth HMAC, rotation N-stations. Stabiliser flaky (fake clock). |
| **F-L1** | `requirements.lock:16-17` | Hygiène | `redis>=5,<7  # not installed`, `gunicorn>=21,<23  # not installed` en commentaire. | Lock incomplet. | **L** | S | Installer ou retirer du lock, aligner env win32 vs linux. |
| **F-L2** | `nginx/nginx.conf` (51 l) | Infra | `keepalive 200` / `keepalive_timeout 30` bien alignés avec `config.yaml`, `proxy_buffering off` SSE correct, `gzip` JSON ok. `listen 80` sans TLS. | Pas de TLS prod. | **L** | M | Ajouter bloc `443 ssl http2` + certs (Let's Encrypt) quand exposé. |
| **F-L3** | `opencode.service` (17 l) | Infra | `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem full`, `ReadWritePaths` bien. `WorkingDirectory /opt/opencode-proxy` + `ExecStart .venv/bin/python opencode.py --no-gui`. | Divergence dev (uvicorn direct) vs prod (gunicorn) — `gunicorn.conf.py` non utilisé par systemd. | **L** | S | Aligner : systemd → gunicorn en prod, uvicorn en dev, ou documenter. |
| **F-L4** | `free_discovery` + `free_model_map` | Dette | `free_model_map` 5 mappings, `free_discovery` HTML filet + cascade `pricing/is_free/.../-free`, `default_target mimo-v2.5-free`. Logique dispersion `opencode.py` + `config/settings.py:416,1142`. | Maintenance discovery. | **L** | M | Centraliser discovery dans `free_discovery.py` dédié, tester cascade. |

---

## 4. Code mort — liste + preuve

| Fichier / pattern | Preuve | Verdict | Action |
|-------------------|--------|---------|--------|
| `free_quota.py` (245 l) | `grep -rn free_quota` → 0 import ; `F821 ×7` (`_free_ip_pool`, `_vpn_manager`, `datetime`…) ; `wc -l 245` | **Mort/cassé** (extraction P3.10 inachevée) | **Supprimer** ou finaliser extraction avec imports/injection (choisir, pas laisser cassé). |
| Artefacts `logs/_*`, `logs/panel_dump.json`, `tmp/`, `pp1.json`, `*_diff_tmp.txt` | `.gitignore` couvre `logs/_*`, `logs/panel_dump.json`, `tmp/`, `pp1.json`, `*_diff_tmp.txt` ; `git ls-files --others` n'en liste pas (déjà ignorés) mais présents sur disque si générés | **Transient** | Ne pas committer, `git clean -fd` si besoin. |
| `tool_compat_results.json` / `tool_capabilities.json` | `.gitignore` les couvre ; non présents en `git ls-files` | **Artefact** | Supprimer du working tree si présent. |
| `.kilo/` 61M | `git status ?? .kilo/` ; `.gitignore` ne le couvre pas | **Hors repo** | Ajouter `.kilo/` à `.gitignore` ou déplacer. |
| Imports morts | `ruff F401 ×9` hors `__init__.py` : `tempfile` (`config/settings.py:10`), `CONFIG_KEYS`/`CUSTOM_ROUTES`/`config.settings`/`os`/`json` (`dashboard/api.py`), `os` (`dashboard/quota.py:8`), `re` (`free_quota.py:6`) | **Mort** | `ruff --fix` ou suppression manuelle. |
| Vars mortes | `F841 ×3` : `status` (`dashboard/api.py:2282`), `group` (2511), `root` (2567) | **Mort** | Supprimer assignation. |
| `dual_station` legacy | `config.yaml:119 dual_station: true` + `station_count: 2` ; code `vpn_manager.py` gère `station_count` 1–10 | **Legacy** | Déprécier `dual_station`, garder `station_count` seul. |
| `server_countries` dupliqué | `config.yaml:138-140` + `docker-compose.yml:35,148,235` + `vpn_manager.py:510` | **Duplication** (pas mort) | Source unique ( §3 F-H5 ). |

**Quantification :** code mort avéré ≈ **245 l** (`free_quota.py`) + **~12 l** imports/vars morts + **61M** `.kilo/` hors repo + **3.28G** bak DB doublon. Dette historique : **342** commentaires datés = **~5–7%** du code en commentaires non normés.

---

## 5. Dette — top 10 + charge estimée

| Rang | Dette | Charge | Priorité |
|------|-------|--------|----------|
| 1 | Découper `opencode.py` 8767 l | XL (2 sem) | Phase 3 |
| 2 | Migrer 342 tags historiques → ADR | L (1 sem) | Phase 2 |
| 3 | Source unique `server_countries` | S (<2h) | Phase 0 |
| 4 | Pinner deps + lock consommé par Dockerfile/CI | S (<2h) | Phase 0 |
| 5 | `free_quota.py` : supprimer ou câbler | S (<2h) | Phase 0 |
| 6 | `pyproject.toml` ruff/mypy/pytest | S (<2h) | Phase 0 |
| 7 | CI gate `ruff` sans `|| true` + `pip-audit`/`gitleaks` | S (<2h) | Phase 1 |
| 8 | DB rotation / VACUUM / logrotate | M (1j) | Phase 1 |
| 9 | Remplacer `threading.Lock+cycle` par `asyncio.Lock` | M (1j) | Phase 1 |
| 10 | Tests SSE/DB/auth/rotation N-stations | M (1j) | Phase 2 |

---

## 6. Roadmap phasée

### Phase 0 — Quick wins (<2h, sans risque)

- [ ] `free_quota.py` : décision — **supprimer** (recommandé, 0 import) ou finaliser extraction (ajouter imports `datetime`, `time`, `asyncio`, `email.utils` + injection `_free_ip_pool`/`_vpn_manager`/`_debug`/`_log`/`_current_free_attempt`). Effacer `re` inutilisé si conservé.
- [ ] `pyproject.toml` minimal (ruff, mypy, pytest, coverage) + `ruff.toml` si besoin.
- [ ] `ruff --fix` sur `config/settings.py:10`, `dashboard/api.py:25,1463,1948,2277`, `dashboard/quota.py:8`, `dashboard/display.py:342`.
- [ ] Re-exports `config/__init__.py` & `dashboard/__init__.py` → `X as X` (ou `__all__`).
- [ ] Source unique `server_countries` : garder `config.yaml` source, template compose `${SERVER_COUNTRIES}` ou génération.
- [ ] Pinner `requirements.txt` depuis `requirements.lock` (pip-tools/uv), faire `Dockerfile` + CI consommer le lock.
- [ ] `.gitignore` : ajouter `.kilo/` (et `.kilo/plans/` si besoin).
- [ ] `git status` propre : `git restore` ou `git add` sur `config.yaml`/`opencode.py` modifiés.

### Phase 1 — Hygiène (1j)

- [ ] CI : retirer `|| true`, ajouter `ruff format --check`, `pytest --cov --cov-fail-under=...`, `pip-audit`, `gitleaks detect`.
- [ ] DB : rotation `requests.db` (retention 30j), `VACUUM` hebdo, `logrotate` `debug.log*`, suppression `requests.db.bak-*` >7j.
- [ ] `threading.Lock` → `asyncio.Lock` pour `_key_cycle` (+ test concurrence).
- [ ] Doc tuning : `WAL`, `mmap 268M`, `busy_timeout 5000`, `poll 5s` vs inotify, `keepalive`/`pool` backpressure.

### Phase 2 — Refactors (1 sem)

- [ ] ADR : migrer 342 tags → `docs/adr/ADR-*.md` + `CHANGELOG.md`, laisser 1 ref par bloc.
- [ ] Dédupliquer `protocol_mapping.py` (`CACHE_REWRITE_MODELS`, `thinking→reasoning_effort`).
- [ ] Centraliser `free_discovery` cascade + `free_model_map` dans module dédié + tests.
- [ ] Tests : SSE e2e, DB WAL, dashboard HMAC, rotation N-stations, fake clock pour flaky `mtime`/`timeout 0.05s`.
- [ ] Déprécier `dual_station` → `station_count` seul.

### Phase 3 — Archi (2 sem)

- [ ] Découper `opencode.py` : `app/router/`, `app/protocol/`, `app/streaming/`, `app/db/`, `app/quotas/` + DI.
- [ ] Unifier VPN/IP pool : `vpn_manager` 3903 l + `free_ip_pool` 1410 l + `shared_rotation` 378 l — extraire `server_scorer`/`nordvpn_api`/`docker_events` en boundaries.
- [ ] Aligner prod : `gunicorn.conf.py` (workers cpu_count, UvicornWorker, keepalive 15, timeout 600) vs `opencode.service` (uvicorn direct) — choisir et documenter.
- [ ] TLS nginx `443 ssl http2` + certs quand exposé.

---

## 7. Sécurité — détail

| Point | État | Recommandation |
|-------|------|----------------|
| `DASHBOARD_TOKEN` HMAC (`dashboard/api.py:76-131`) | Warning si `0.0.0.0` sans token, `hmac.compare_digest` ok | Passer en fail-closed optionnel (`DASHBOARD_REQUIRE_TOKEN=true`). Doc `X-Dashboard-Token`. |
| `control_api_key: ''` (`config.yaml:175`) | Vide par défaut | Générer au boot si vide, persister dans `config.yaml` ou `credentials.env`. |
| `0.0.0.0:4000` (`config.yaml:2`, `dashboard/api.py:90-93`) | 9 occurrences `0.0.0.0` | Bindre `127.0.0.1` par défaut, `0.0.0.0` seulement derrière nginx/auth. |
| `docker.sock` mount (`docker-compose.yml:96`) | Nécessaire pour contrôle gluetun | Restreindre : socket proxy (`docker-socket-proxy`) ou `DOCKER_GID` minimal. |
| `cap_add: NET_ADMIN` ×4 (`docker-compose.yml:7,111,202,431`) | Requis gluetun | Ok, mais documenter. Pas de `privileged`. |
| `ReadWritePaths` systemd (`opencode.service:15`) | `PrivateTmp`, `ProtectSystem full`, `NoNewPrivileges` | Bien. Ajouter `ProtectHome true` déjà présent. |
| `max_body_size 10485760` (`config.yaml:20`, `nginx:client_max_body_size 10m`) | Aligné | Ok. |
| Secrets gitignored | `.env`, `credentials.env`, `vpn_configs/*.ovpn`, `logs/*.db*` | Ok. Vérifier `gitleaks` en CI. |

---

## 8. Perf — détail

- **Bien :** `orjson` fast-path, `httpx[socks,http2]` pool `max_connections 500`/`max_keepalive 200`/`keepalive_expiry 30`, `uvloop`/`httptools` (non-Windows), DB WAL (`journal_mode WAL`, `synchronous NORMAL`, `cache_size -64000 ~256M`, `mmap 268M`), `gunicorn` `workers cpu_count` `UvicornWorker` `worker_connections 2000` `keepalive 15` `timeout 600`, nginx `proxy_buffering off` SSE + `keepalive 200` + `gzip` JSON + `http2`.
- **À mesurer :** backpressure pool 500 sans doc, `CACHE_MIN_PROMPT_SIZE 2000`/`max_size 1000` TTL 300 non benchmarké, DB `mmap 268M` vs `cache_size`, `health_cache_ttl 15` vs `status_cache 2s`, `wal_checkpoint_interval 3600`.

---

## 9. Tests — inventaire

- **38 fichiers** `tests/` (13 k lignes) : `test_vpn_freshness` 1487 l, `test_gluetun_control_api` 1127 l, `test_proxy` 827 l, `test_free_multi_attempt` 699 l, `test_invariant_a0` 633 l, `test_vpn_endpoints` 593 l, `test_rotation_concurrency` 552 l, `test_pool_connection_failure` 505 l, `test_env_staleness_guard` 462 l, `test_traffic_capture` 450 l, `test_geo_routing` 437 l, `test_tool_compat` 403 l, `test_free_discovery` 378 l, etc.
- `conftest.py` autouse `_reset_disable_mapping` + `pytest_asyncio`.
- Scripts `scripts/smoke_todo10.py` 328 l, `scripts/vpn_e2e_smoke.py` 340 l non gatés CI.
- **Manques :** streaming SSE réel, DB WAL, dashboard auth HMAC, rotation N-stations e2e, flaky `asyncio.run + timeout 0.05s + mtime sleep 0.02` (à stabiliser fake clock).

---

## 10. Annexes — commandes reprodu

```bash
# Tailles & hotspots
wc -l opencode.py vpn_manager.py dashboard/api.py free_ip_pool.py config/settings.py protocol_mapping.py | sort -rn

# Tags historiques
grep -rn "\[plan\|\[fix\|\[incident\|\[audit\|\[Axe\|\[CRITIC" --include="*.py" --include="*.yml" | wc -l
grep -rn "TODO\|FIXME\|HACK\|XXX" --include="*.py" --include="*.yml" | sort

# Ruff (hors re-exports __init__.py)
python -m ruff check . --select F401,F841,F821 --output-format github | grep -v "__init__.py"
python -m ruff check . --output-format github | head -60

# Hygiene git
git status --short
git ls-files --others --exclude-standard | head -30
git diff --stat HEAD

# Deps
pip list --outdated | head -40
cat requirements.txt
cat requirements.lock

# Docker / gunicorn
docker compose config | head -80
gunicorn -c gunicorn.conf.py opencode:app --check-config

# Tests
pytest -q --tb=short
pytest --cov --cov-report=term-missing  # si pytest-cov installé
```

### Sorties de référence (2026-08-23)

- `ruff F401/F841/F821` : 324 totaux dont ~59 `__init__.py` re-exports, **19 vrais** hors `__init__.py` (9 F401 + 3 F841 + 7 F821).
- `vulture` : non installé (à ajouter).
- `pip-audit` : non installé (à ajouter).
- `pip list --outdated` : 30+ majeures en retard (aiohttp 3.13→3.14, anthropic 0.46→1.0, etc.) — pinner avant bump.
- `gitleaks` : non installé (à ajouter).
- `git diff --stat HEAD` : `config.yaml | 2 +-`, `opencode.py | 140 ++++++++++++++++++++++++ 117 +25 -`.

---

## 11. Décision & next step

**Gate actuel :** audit seul — **aucune modif prod** sans approbation du rapport.

**Next step recommandé :** revue du rapport avec le demandeur, arbitrage `free_quota.py` (supprimer vs câbler), puis **Phase 0** (<2h) en une PR `chore/audit-phase0` (pyproject + ruff fix + .gitignore + pin deps + server_countries source unique).

*Rapport généré le 2026-08-23 — à versionner dans `docs/audit-2026-08-23.md`.*
