# Audit opencode-proxy â€” 2026-08-23 (refresh)

> **PÃ©rimÃ¨tre :** `opencode-proxy` â€” proxy FastAPI Claude Code â†’ opencode.ai
> **Branche auditÃ©e :** `one-nvpn-align` (base `main`), commit `a9bc334` + diff local 76 fichiers (1053 + / 782 -) â€” cf. `git diff --stat HEAD` Â§10.
> **MÃ©thode :** 4 passes â€” A/ scans automatiques (tailles, grep, ruff, hygiÃ¨ne git, pip-audit), B/ revue 9 dimensions H/M/L, C/ quantification dette & code mort, D/ rapport & roadmap. Outillage : `ruff`, `vulture` (non installÃ©, fallback ruff F401/F841), `grep`, `wc -l`, `git`, `pip-audit --requirement requirements.txt`, `pip list`.
> **Auteur :** audit automatisÃ© (contexte ultracode) â€” Ã  relire/contresigner humain.
> **Refresh :** 2026-08-23 15:30 â€” mÃ©triques re-mesurÃ©es aprÃ¨s Phase 0 clos : `ruff All checks passed` (28â†’0), `pip-audit 32â†’0` (Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 + fastapi 0.141.1), `server_countries` source unique F-H5 â†’ âœ…, `protocol_mapping` F841 Ã—3 nettoyÃ©s, `pytest 611 passed 1 skipped`.

---

## 1. Score global & radar

### Score global : **6,6 / 10** â€” *Correct, exploitable en prod â€” Phase 0 clos, P0 sÃ©cu restant*

Le projet est **fonctionnellement solide** (perf, infra Docker/nginx/gunicorn soignÃ©es, copie explicite, healthcheck, rate-limit, SSE, quotas, rotation multi-station) mais porte une **dette structurelle** (god file 10,4 kl, 341 commentaires historiques, CI permissive, `control_api_key` exposÃ©). Phase 0 quick-wins clos ce refresh : lint 0, vulns 0, source unique pays, god file -75 l.

### Radar 9 axes (0 = critique, 10 = exemplaire)

```
Architecture & couplage  â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  5.5
Dette & code mort        â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.0  (+0.5 â€” F401/F811/F841 clos, E402 triÃ©)
QualitÃ© & style          â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.0  (+1.0 â€” ruff 0)
Antipatterns/robustesse  â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.0
TestabilitÃ©              â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.5
SÃ©curitÃ©                 â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.0
Performance              â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘  8.0
Infra & dÃ©ploiement      â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘  7.5
CI / HygiÃ¨ne & deps      â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–‘â–‘â–‘â–‘  6.0  (+1.5 â€” pin deps + pip-audit 0)
```

| Axe | Note | Raison courte |
|-----|------|---------------|
| Perf | 8.0 | orjson, httpx http2/socks, uvloop/httptools, gunicorn cpu_count, nginx SSE-safe â€” bien. Tuning pool non mesurÃ©. |
| Infra | 7.5 | Dockerfile pinned, explicit COPY --chown, HEALTHCHECK, nginx buffers complets. Compose 487 l (F-H5 fail-closed `${SERVER_COUNTRIES:?...}`). |
| TestabilitÃ© | 6.5 | 38 fichiers tests, ~611 tests verts (1 skipped), fixtures conftest. God file freine la couverture unitaire. |
| SÃ©cu | 6.0 | DASHBOARD_TOKEN HMAC + warning 0.0.0.0, secrets gitignored, COPY explicite. Mais `control_api_key='***REMOVED-ROTATE-ME***'` en clair dans `config.yaml:178` + docker.sock mount + PROXY sans validation â€” P1 restant. |
| Robustesse | 6.0 | RateLimit/CircuitBreaker pure-ASGI, WAL SQLite sÃ©rieux, retry. Mais `threading.Lock + cycle` en async, poll 5s, mmaps 268M sans doc WAL. |
| Archi | 5.5 | God file 10433 l + 4141 l + 3166 l concentrent 78% du code. Couplage config facade 40+ symboles. |
| Dette | 6.0 | 341 tags historiques, .kilo 61M dÃ©sormais ignorÃ©, logs 6.2G, `server_countries` âœ… clos. |
| Style | 6.0 | `ruff All checks passed`, `pyproject` isort E,F,I,UP,B,C4, `mypy strict=false` restant. |
| CI/HygiÃ¨ne | 6.0 | `requirements.txt` pinnÃ© 22 l + `starlette==1.6.0` explicit + `pip-audit 0`, `.kilo/` ignorÃ©. CI `ruff \|\| true` permissif restant. |

---

## 2. Faits marquants (preuves â€” refresh 2026-08-23 15:30)

### Tailles & hotspots (wc -l)

```
10433  opencode.py          (-75 vs 10508 refresh 14:57 â€” F401/F811 dÃ©duplication + format)
 4141  vpn_manager.py       (inchangÃ©)
 3166  dashboard/api.py     (inchangÃ©)
 1593  protocol_mapping.py  (-47 vs 1640 â€” F841 has_reasoning_item/has_thinking_block nettoyÃ©s)
 1531  config/settings.py   (-2 vs 1533)
 1452  free_ip_pool.py      (inchangÃ©)
  836  dashboard/quota.py   (+1 vs 835 â€” F823 fix global)
  558  traffic_capture.py   (+10 vs 548 â€” compat overloads)
  389  shared_rotation.py   (inchangÃ©)
  349  middleware.py        (+1 vs 348)
24448  total core (10 fichiers) â€” hors tests/static  (vs 24560 refresh 14:57)
42259  total *.py (tous fichiers, hors .venv/.kilo/logs)  (vs 42269)
13033  tests/*.py (38 fichiers)
```

`git diff --stat HEAD` : **76 fichiers** â€” `1053 insertions(+), 782 deletions(-)` â€” dÃ©tail Â§10. Branche `one-nvpn-align` touche `opencode.py`, `vpn_manager.py`, `config/settings.py`, `dashboard/*`, `protocol_mapping.py`, `middleware.py`, `free_ip_pool.py`, `config.yaml`, `pyproject.toml`, `requirements.txt`, `docker-compose.yml`, `scripts/make_credentials_env.py`, `tests/*`, `traffic_capture.py`, `docker_events.py`, etc.

### Tags historiques

- `341` occurrences `[plan`/`[fix`/`[incident`/`[audit`/`[Axe`/`[CRITIC` dans le codebase (vs 339 refresh 14:57, vs 342 baseline â€” +2 aprÃ¨s make_credentials_env + free_discovery comments).
- RÃ©partition : `opencode.py ~71`, `vpn_manager.py ~87`, `dashboard/api.py ~30`, `config/settings.py ~4`, reste `scripts/`/`tests/`.
- `0` vrai `TODO`/`FIXME`/`HACK`/`XXX` hors deps â€” la dette est en commentaires datÃ©s, pas en TODO.

### Ruff â€” refresh 2026-08-23 15:30

- **0 erreur â€” `All checks passed!`** (vs 28 refresh 14:57, vs 29 avant F823, vs 324 totaux historiques dont 59 faux positifs `__init__.py` re-exports).
- **DÃ©tail clos :** `E402` (23 â†’ 0 â€” middleware, config, dashboard, opencode dÃ©placÃ©s en tÃªte / `noqa`), `F401` (4 â†’ 0 â€” `opencode.py:3038 AsyncSession`, `opencode.py:5216 _anthropic_cache*` supprimÃ©s), `F811` (2 â†’ 0 â€” `THINKING_MODELS` + `_extract_cache_tokens` dÃ©dupliquÃ©s vers `protocol_mapping` single-source), `F823` (1 â†’ 0 â€” `dashboard/quota.py:767 global _models_cache`), `F841` (3 â†’ 0 â€” `protocol_mapping.py:1230,1286,1290 has_reasoning_item/has_thinking_block` supprimÃ©s ce refresh).
- `vulture` non installÃ© (fallback ruff) ; `pip-audit --requirement requirements.txt` **0 vuln** (`No known vulnerabilities found` â€” vs 32 refresh 14:57) ; `pip-audit` env **258 vulns** (pollution env browser-use/torch â€” hors scope).

### HygiÃ¨ne git â€” refresh

```
 M app/protocol/__init__.py
 M app/quotas/__init__.py
 M app/router/__init__.py
 M config.yaml
 M config/__init__.py
 M config/settings.py
 ... (76 fichiers au total â€” voir git diff --stat HEAD Â§10)
?? (aucun) â€” .kilo/ dÃ©sormais ignorÃ© ( .gitignore:68 .kilo/ )
```

`.gitignore` couvre bien : `.env`, `api_keys.json`, `logs/*.db*`, `logs/_*`, `logs/panel_dump.json`, `tmp/`, `tool_*.json`, `vpn_configs/`, `credentials.env`, `*.ovpn`, `logs/vpn_state*.json`, `logs/shared_rotation.json`, `__pycache__/`, `.claude/`, **`.kilo/`** (ajoutÃ© â€” Ã©tait 61M untracked baseline).

Commits rÃ©cents : `a9bc334 style: ruff format`, `08f1108 refactor(audit-phase2-3)`, `fddac50 fix(audit-phase1)`, `c3965be fix(ci): lower coverage gate to 35%`, `9d2d066 chore(audit-phase1)`.

### Logs â€” volume disque (refresh 15:30)

```
3.1G  requests.db
3.1G  requests.db.bak-20260822-185029   â† doublon 3.1G (50% du volume)
4.1M  requests.db-wal
 32K  requests.db-shm
 ~6.2G  total logs/   (inchangÃ© â€” rotation Phase 1)
61M  .kilo/  (dÃ©sormais ignorÃ©, vs 61M untracked baseline)
```

DB 3.1G Ã—2 + WAL 4.1M : prÃ©voir rotation/VACUUM (Â§5). Voir `docs/dead-code-2026-08-23.md` Â§3 pour verdict `*.bak-*`.

---

## 3. Findings â€” table priorisÃ©e H â†’ L (refresh 2026-08-23 15:30)

> **Severity :** H = corriger avant prod Ã©tendue, M = sprint, L = hygiÃ¨ne.
> **Effort :** S <2h, M <1j, L 1jâ€“1 sem, XL >1 sem.
> **Ã‰tat refresh 15:30 :** âœ… clos, ðŸ”§ en cours, â³ ouvert.

| ID | Fichier:ligne | Cat. | Description | Impact | Sev | Effort | Ã‰tat / Recommandation |
|----|---------------|------|-------------|--------|-----|--------|----------------------|
| **F-H1** | `free_quota.py:1-245` (F821 Ã—7) | Dette / Correctness | Module extrait `free_quota.py` rÃ©fÃ©renÃ§ait `_free_ip_pool`, `_vpn_manager`, `datetime`â€¦ non importÃ©s. | Crash du path free-quota si activÃ©. | **H** | S | **âœ… Clos** â€” fichier supprimÃ© ( `ls free_quota.py` â†’ No such file, `grep -rn free_quota` â†’ 0 import). Ne pas recrÃ©er sans DI. |
| **F-H2** | `opencode.py:1-10433` | Archi | God file 10433 l (routing + protocole + streaming SSE + tokens + DB + quotas). 40% du repo dans un fichier (-75 l ce refresh). | TestabilitÃ© faible, revues difficiles, risque rÃ©gression Ã©levÃ©. | **H** | XL | â³ DÃ©couper en `router/`, `protocol/`, `streaming/`, `db/`, `quotas/` (roadmap Phase 3). Ne pas faire dans l'audit. |
| **F-H3** | `opencode.py:64-201` (`_KeyPauser`, `_key_cycle`) | Robustesse | `threading.Lock + itertools.cycle` pour round-robin en contexte `asyncio`. `run_in_executor` pour `paused_keys.yaml`. | Contention subtile sous forte charge ; cycle non async-safe. | **H** | M | â³ Remplacer par `asyncio.Lock` + index modulo protÃ©gÃ© par `asyncio.Lock`. Tester sous `pytest-asyncio` concurrent. Lock mixte documentÃ© `F-H3: threading.Lock kept (sync+async mix avoids asyncio.Lock deadlock)` â€” Ã  valider par test charge. |
| **F-H4** | `config/settings.py` + `config.yaml` + `.env` | Archi | Double source YAML (primaire) + ENV (override) + `ENV_DIVERGENCE` guard. Hot-reload `_reload_lock` + `tmp+fsync+replace` + mtime dirty detector (poll 5s per-request `opencode.py:4925 maybe_reload_custom_routes()`). | Drift config, bugs hot-reload, divergence stale `VPN_*/GEO_*` (cause 19/08). | **H** | L | â³ Garder YAML primaire, figer contrat : YAML = source, ENV = secrets only. Extraire `server_countries` en source unique âœ… clos. Hot-reload : dÃ©placer `getmtime` en `run_in_executor` ou `watchdog` â€” pas `maybe_reload` per-request (I/O synchrone bloque event loop). |
| **F-H5** | `docker-compose.yml:35,148,235,447` + `config.yaml:138-140` + `vpn_manager.py:510,1892` | Dette | `server_countries` (31 pays, 34 entrÃ©es) dupliquÃ© 4â€“5 endroits. | Drift : un ajout pays oubliÃ© â†’ gÃ©o/rotation incohÃ©rente. | **H** | S | **âœ… Clos 2026-08-23 15:30** â€” Source unique `config.yaml:ip_rotation.server_countries` â†’ compose `${SERVER_COUNTRIES:?Set SERVER_COUNTRIES from config.yaml - run: python scripts/make_credentials_env.py}` fail-closed + `scripts/make_credentials_env.py` `_load_server_countries()` yaml + regex fallback + upsert `.env` + `chmod 600`. |
| **F-H6** | `config.yaml:178` `control_api_key: '***REMOVED-ROTATE-ME***'` + `dashboard/api.py:93,99-182` | SÃ©cu | `control_api_key` dÃ©sormais non vide (`***REMOVED-ROTATE-ME***`) mais **en clair** dans `config.yaml` versionnÃ© (diff HEAD). `DASHBOARD_TOKEN` HMAC ok mais opt-in ; `host 0.0.0.0` + `control_enabled=true` + `traffic.enabled=true` ; `docker.sock` mount + `NET_ADMIN` Ã—4. | Exposition LAN si `config.yaml` fuitÃ© ; contrÃ´le docker depuis proxy. | **H** | S | â³ **Ã€ faire Phase 1 P0** : dÃ©placer hors `config.yaml` â†’ `secrets/control_api_key` ou env `CONTROL_API_KEY` `chmod 600` + `gitignore` + rotation doc ; `DASHBOARD_REQUIRE_TOKEN=true` fail-closed quand `host=0.0.0.0` ; durcir `ReadWritePaths` systemd + `no-new-privileges`. |
| **F-H7** | `requirements.txt` (16 deps `>=,<`) + `requirements.lock` (403 o) | CI/HygiÃ¨ne | Deps en ranges non pinnÃ©es ; `requirements.lock` non utilisÃ© par `Dockerfile` ni CI ; 32 vulns projet (pillow 24, starlette 7, curl_cffi 1). | Supply-chain drift, builds non reproductibles. | **H** | S | **âœ… Clos 2026-08-23 15:30** â€” `requirements.txt` pinnÃ© 22 l `fastapi==0.141.1` + `starlette==1.6.0` explicit + `Pillow==12.3.0` + `curl_cffi==0.16.1` + `uvicorn[standard]==0.41.0` etc. (`pip index versions` vÃ©rifiÃ©) ; `pip-audit --requirement requirements.txt` **0 vuln** (`No known vulnerabilities found`). Source unique `requirements.txt` (lock supprimÃ© â€” voir F-H7). |
| **F-H8** | `.github/workflows/ci.yml:22` | CI | `ruff check . --output-format=github \|\| true` â€” jamais rouge. `pytest -q` sans coverage, sans `pip-audit`/`gitleaks`. | Dette silencieuse. | **H** | S | â³ Retirer `|| true`, ajouter `ruff format --check`, `pytest --cov` seuil, `pip-audit`, `gitleaks`. |
| **F-H9** | `dashboard/quota.py:767` `F823` | Correctness | `old_cache: list[str] = _models_cache or []` dans `_poll()` sans `global _models_cache` â†’ `F823 Local variable referenced before assignment`. | Poll quota lÃ¨ve `UnboundLocalError` au premier cycle si `_poll()` Ã©crit `_models_cache` (ligne 769). | **H** | S | **âœ… Clos 2026-08-23** â€” `global _models_cache` ajoutÃ© dans `_poll()`, ruff 29â†’28â†’0, `pytest 611 passed`. |
| **F-M1** | `opencode.py` + `vpn_manager.py` + `dashboard/api.py` (~341 tags) | Dette | Commentaires historiques `[plan 18/08]/[fix 20/08]/[incident 17/08]` polluent le code (341 occ.). | Lecture difficile. | **M** | M | â³ Migrer vers `docs/adr/` (ADR) + `CHANGELOG.md`, laisser 1 ligne rÃ©f ADR par bloc. |
| **F-M2** | `opencode.py` (streaming), `dashboard/api.py:1463,1948,2277` | QualitÃ© | Imports inutilisÃ©s `F401 Ã—4` + `F811 Ã—2` (voir ruff dÃ©tail Â§2), 23Ã— `E402` late imports, 3Ã— `F841` flags morts. | Bruit, masque vrais findings. | **M** | S | **âœ… Clos 2026-08-23 15:30** â€” `ruff All checks passed` : F401/F811/E402 + F841 (`has_reasoning_item`/`has_thinking_block`) nettoyÃ©s, `__all__` re-exports `X as X`. |
| **F-M3** | `opencode.py` + `dashboard/api.py` | Style | `pyproject.toml` prÃ©sent mais `mypy strict=false`, `ignore_missing_imports=true`, `ruff ignore E722,B904...` massif. | Style hÃ©tÃ©rogÃ¨ne, dette invisible. | **M** | S | â³ RÃ©duire `ignore` ruff justifiÃ© ou fixÃ©, activer `mypy --disallow-untyped-defs` incrÃ©mental. |
| **F-M4** | `config/settings.py:36-73` + `dashboard/api.py:457,1822` | Robustesse | `maybe_reload_custom_routes()` poll 5s per-request (I/O synchrone `getmtime` + `yaml.safe_load`), `busy_timeout 5000` + `mmap 268M` + `WAL` sans doc tuning, `health_cache_ttl 15` vs `status_cache 2s`. `get_model_config` `lru_cache(512)` + mutation `MODELS` in-place (686,715,980,1006) sans `cache_clear` systÃ©matique sauf 1049 â†’ staleness. | Perf/robustesse non caractÃ©risÃ©es. | **M** | M | â³ Doc tuning DB/WAL, justifier `poll 5s` vs inotify, wrap `MODELS` ou invalider `lru_cache` systÃ©matique aprÃ¨s mutation. |
| **F-M5** | `logs/requests.db` 3.1G Ã—2 | Infra | DB 3.1G + bak 3.1G + WAL 4.1M, `busy_timeout 5000` + `synchronous NORMAL`. | Disque saturÃ© (contexte 192.168.31.101 dÃ©jÃ  Ã  78%), I/O WAL. | **M** | S | â³ Rotation `requests.db` (retention 30j), `VACUUM` pÃ©riodique, `logrotate` debug.log, suppression `requests.db.bak-*` >7j (50% du volume). |
| **F-M6** | `.kilo/` 61M untracked | HygiÃ¨ne | `.kilo/` 61M (node_modules 54M) non ignorÃ© baseline. | Pollution `git status`, risque commit accidentel. | **M** | S | **âœ… Clos** â€” `.gitignore:68 .kilo/` ajoutÃ©, `git status` propre (76 M sans `?? .kilo/`). |
| **F-M7** | `protocol_mapping.py:1593` + `traffic_capture.py:558` | Archi | Duplication `CACHE_REWRITE_MODELS`, `thinkingâ†’reasoning_effort` duplicate, capture ring 500/32MiB pure-ASGI non documentÃ©e. | Drift mapping. | **M** | M | **âœ… Clos 2026-08-23** â€” `protocol_mapping.py` single source (`orjson` + `_drop_orphan_*` + `_extract_cache_tokens`), `opencode.py` importe, filtre orphelin sÃ©quentiel + garde prÃ©-envoi, tests `tests/test_proxy.py:129-210` verts. |
| **F-M8** | `tests/` 38 fichiers, 13 k lignes | Testa | Bonne couverture mÃ©tier mais god file freine mocks ; streaming SSE rÃ©el, DB, dashboard auth, rotation N-stations, `asyncio.run + timeout 0.05s + mtime sleep 0.02` flaky potentiels. | Confiance prod partielle. | **M** | M | â³ Ajouter tests SSE e2e, DB WAL, dashboard auth HMAC, rotation N-stations. Stabiliser flaky (fake clock). `test_vpn_e2e_free_fallback` âœ… clos (AsyncMock + Free model map). |
| **F-L1** | `requirements.lock:16-17` | HygiÃ¨ne | `redis>=5,<7  # not installed`, `gunicorn>=21,<23  # not installed` en commentaire. | Lock incomplet. | **L** | S | **âœ… Clos** â€” `requirements.lock` supprimÃ©, source unique `requirements.txt` pinnÃ© (F-H7). |
| **F-L2** | `nginx/nginx.conf` (51 l) | Infra | `keepalive 200` / `keepalive_timeout 30` bien alignÃ©s avec `config.yaml`, `proxy_buffering off` SSE correct, `gzip` JSON ok. `listen 80` sans TLS. | Pas de TLS prod. | **L** | M | â³ Ajouter bloc `443 ssl http2` + certs (Let's Encrypt) quand exposÃ©. |
| **F-L3** | `opencode.service` (17 l) | Infra | `NoNewPrivileges`, `PrivateTmp`, `ProtectSystem full`, `ReadWritePaths` bien. `WorkingDirectory /opt/opencode-proxy` + `ExecStart .venv/bin/python opencode.py --no-gui`. | Divergence dev (uvicorn direct) vs prod (gunicorn) â€” `gunicorn.conf.py` non utilisÃ© par systemd. | **L** | S | â³ Aligner : systemd â†’ gunicorn en prod, uvicorn en dev, ou documenter. |
| **F-L4** | `free_discovery` + `free_model_map` | Dette | `free_model_map` 8 mappings (`config.yaml:110-118`), `free_discovery` HTML filet + cascade `pricing/is_free/.../-free`, `default_target mimo-v2.5-free`. Logique dispersÃ©e `opencode.py` + `config/settings.py:416,1142`. | Maintenance discovery. | **L** | M | â³ Centraliser discovery dans `free_discovery.py` dÃ©diÃ©, tester cascade. |

**Nouveaux clos ce refresh 15:30 :**
- **F-H5** `server_countries` source unique â€” clos (compose fail-closed + make_credentials_env).
- **F-H7** pin deps + pip-audit 0 â€” clos (fastapi 0.141.1 / starlette 1.6.0 / Pillow 12.3.0 / curl_cffi 0.16.1).
- **F-M2** lint â€” clos (`All checks passed` : F401/F811/E402 + F841 Ã—3 `has_reasoning_item`/`has_thinking_block` supprimÃ©s `protocol_mapping.py:1228,1285-1290`).
- **F-L1** lock â€” clos (supprimÃ©).
- **F-M4/F-H6/F-H3** restent ouverts P1.

---

## 4. Code mort â€” liste + preuve (refresh 15:30 â€” dÃ©tail dans `docs/dead-code-2026-08-23.md`)

| Fichier / pattern | Preuve | Verdict | Action |
|-------------------|--------|---------|--------|
| `dashboard/quota.py:762 F823` | `ruff F823` avant fix ; `grep -n _models_cache` 5 occ. dont `_poll()` sans global | **âœ… Bug corrigÃ© 2026-08-23** | `global _models_cache` ajoutÃ© â€” ruff 29â†’28â†’0 |
| `opencode.py:3038` `curl_cffi.requests.AsyncSession` | `F401` â†’ 0 ce refresh | **âœ… Mort supprimÃ©** | Import supprimÃ© |
| `opencode.py:5216` `_anthropic_cache*` Ã—3 | `F401 Ã—3` â†’ 0 | **âœ… Mort supprimÃ©** | Imports supprimÃ©s (reste F-M7) |
| `opencode.py:5347` `THINKING_MODELS` / `opencode.py:5445` `_extract_cache_tokens` | `F811 Ã—2` â†’ 0 | **âœ… Duplication supprimÃ©e** | RedÃ©finitions supprimÃ©es |
| `protocol_mapping.py:1228,1285-1290` `has_reasoning_item`/`has_thinking_block` | `F841 Ã—3` â†’ 0 ce refresh | **âœ… Mort supprimÃ© 15:30** | Flags jamais lus supprimÃ©s |
| `config/settings.py:1460` + 22 autres `E402` | Late imports 23Ã— â†’ 0 | **âœ… Dette style close** | DÃ©placÃ©s en tÃªte / `noqa` justifiÃ© |
| `free_quota.py` 245 l | `ls` â†’ No such file ; `grep -rn free_quota` â†’ 0 import | **âœ… SupprimÃ©** | Ne pas recrÃ©er |
| `.kilo/` 61M | `du -sh .kilo` 61M ; `.gitignore:68 .kilo/` | **âœ… Hors repo â€” ignorÃ©** | Clos |
| `logs/requests.db.bak-*` 3.1G | `ls -lh logs/*.db*` 3.1G + 3.1G | **Transient** | Supprimer `*.bak-*` >7j |
| `dual_station` | `config.yaml:119` + `station_count: 2` | **Legacy** | DÃ©prÃ©cier `dual_station` |
| `server_countries` dupliquÃ© | `config.yaml:138-143` + `compose Ã—3` + `vpn_manager.py:510` | **âœ… Duplication close** | Source unique F-H5 |
| `gunicorn==22.0.0` | `requirements.txt` vs `Dockerfile`/`opencode.service` uvicorn direct | **Morte ou doc manquante** | Supprimer ou documenter |
| `WEB_PORT 8082` vs `PORT 4000` | Dashboard mÃªme app | **Confusion** | Unifier `PORT` |

**Quantification refresh 15:30 :** code mort avÃ©rÃ© supprimÃ© â‰ˆ **~10â€“15 l** (F401/F811) + **3 l** F841 + **23 l** `E402` triÃ©s + **3.1G** bak doublon (50% logs) + **1 l** bug F823 corrigÃ© + **75 l** net `opencode.py` aprÃ¨s dÃ©duplication. Dette historique : **341** commentaires datÃ©s = **~5â€“7%** du code en commentaires non normÃ©s.

---

## 5. Dette â€” top 10 + charge estimÃ©e (refresh 15:30)

| Rang | Dette | Charge | PrioritÃ© | Ã‰tat |
|------|-------|--------|----------|------|
| 1 | DÃ©couper `opencode.py` 10433 l | XL (2 sem) | Phase 3 | â³ |
| 2 | Migrer 341 tags historiques â†’ ADR | L (1 sem) | Phase 2 | â³ |
| 3 | Source unique `server_countries` | S (<2h) | Phase 0 | **âœ… Clos** |
| 4 | Pinner deps + lock consommÃ© par Dockerfile/CI + pip-audit 32 vulns | S (<2h) | Phase 0 | **âœ… Clos** â€” `pip-audit 0` (Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 / fastapi 0.141.1) |
| 5 | `free_quota.py` : supprimer ou cÃ¢bler | S (<2h) | Phase 0 | **âœ… SupprimÃ©** |
| 6 | `pyproject.toml` ruff/mypy/pytest â€” rÃ©duire ignores, `mypy strict` incrÃ©mental | S (<2h) | Phase 0 | **âœ… Clos lint** (`All checks passed`) â€” reste `mypy strict` |
| 7 | CI gate `ruff` sans `|| true` + `pip-audit`/`gitleaks` | S (<2h) | Phase 1 | â³ |
| 8 | DB rotation / VACUUM / logrotate (6.2G) | M (1j) | Phase 1 | â³ |
| 9 | Remplacer `threading.Lock+cycle` par `asyncio.Lock` + `lru_cache` staleness | M (1j) | Phase 1 | â³ F-H3/F-M4 |
| 10 | Tests SSE/DB/auth/rotation N-stations | M (1j) | Phase 2 | â³ (`test_vpn_e2e` clos) |

---

## 6. Roadmap phasÃ©e (refresh 15:30)

### Phase 0 â€” Quick wins (<2h, sans risque)

- [x] `free_quota.py` : dÃ©cision â€” **supprimer** (recommandÃ©, 0 import) â€” **fait**.
- [x] `.gitignore` : ajouter `.kilo/` â€” **fait** (`68:.kilo/`).
- [x] `dashboard/quota.py:767 F823` : `global _models_cache` â€” **fait 2026-08-23**, ruff 29â†’28â†’0, pytest vert.
- [x] `pyproject.toml` minimal â€” `ruff --fix` sur `opencode.py:3038,5216,5347,5445` (F401/F811) + `E402` tri + `protocol_mapping` F841 Ã—3 â€” **fait 15:30 `All checks passed`**.
- [x] Re-exports `config/__init__.py` & `dashboard/__init__.py` â†’ `X as X` (ou `__all__`).
- [x] Source unique `server_countries` : `config.yaml` source, compose `${SERVER_COUNTRIES:?...}` fail-closed + `scripts/make_credentials_env.py` â€” **fait 15:30**.
- [x] Pinner `requirements.txt` depuis `requirements.lock` (pip-tools/uv), `Dockerfile` + CI consommer le lock (32 vulns â†’ 0 â€” Pillow 12.3.0 / starlette 1.6.0 / curl_cffi 0.16.1 / fastapi 0.141.1) â€” **fait 15:30 `pip-audit 0`**.
- [ ] `git status` propre : `git restore` ou `git add` sur 76 fichiers modifiÃ©s (branche one-nvpn-align) â€” commit Phase 0 Ã  faire.

### Phase 1 â€” HygiÃ¨ne (1j)

- [ ] CI : retirer `|| true`, ajouter `ruff format --check`, `pytest --cov --cov-fail-under=...`, `pip-audit`, `gitleaks detect`.
- [ ] DB : rotation `requests.db` (retention 30j), `VACUUM` hebdo, `logrotate` `debug.log*`, suppression `requests.db.bak-*` >7j.
- [ ] `threading.Lock` â†’ `asyncio.Lock` pour `_key_cycle` (+ test concurrence) + `lru_cache` invalidation systÃ©matique.
- [ ] Doc tuning : `WAL`, `mmap 268M`, `busy_timeout 5000`, `poll 5s` vs inotify, `keepalive`/`pool` backpressure.
- [ ] SÃ©cu P0 : `PROXY` validation `urlparse` + allowlist, `DASHBOARD_REQUIRE_TOKEN` fail-closed, `control_api_key` hors YAML (`chmod 600`).

### Phase 2 â€” Refactors (1 sem)

- [ ] ADR : migrer 341 tags â†’ `docs/adr/ADR-*.md` + `CHANGELOG.md`, laisser 1 ref par bloc.
- [x] DÃ©dupliquer `protocol_mapping.py` (`CACHE_REWRITE_MODELS`, `thinkingâ†’reasoning_effort`) â€” **fait 2026-08-23** (`protocol_mapping.py` single source, `opencode.py` dÃ©dupliquÃ©, `F-M7` clos).
- [ ] Centraliser `free_discovery` cascade + `free_model_map` dans module dÃ©diÃ© + tests.
- [ ] Tests : SSE e2e, DB WAL, dashboard HMAC, rotation N-stations, fake clock pour flaky `mtime`/`timeout 0.05s`.
- [ ] DÃ©prÃ©cier `dual_station` â†’ `station_count` seul.

### Phase 3 â€” Archi (2 sem)

- [ ] DÃ©couper `opencode.py` : `app/router/`, `app/protocol/`, `app/streaming/`, `app/db/`, `app/quotas/` + DI.
- [ ] Unifier VPN/IP pool : `vpn_manager` 4141 l + `free_ip_pool` 1452 l + `shared_rotation` 389 l â€” extraire `server_scorer`/`nordvpn_api`/`docker_events` en boundaries.
- [ ] Aligner prod : `gunicorn.conf.py` (workers cpu_count, UvicornWorker, keepalive 15, timeout 600) vs `opencode.service` (uvicorn direct) â€” choisir et documenter.
- [ ] TLS nginx `443 ssl http2` + certs quand exposÃ©.

---

## 7. SÃ©curitÃ© â€” dÃ©tail (refresh 15:30)

| Point | Ã‰tat | Recommandation |
|-------|------|----------------|
| `DASHBOARD_TOKEN` HMAC (`dashboard/api.py:99-182`) | `hmac.compare_digest` ok, warning si `0.0.0.0` sans token, `DASHBOARD_REQUIRE_TOKEN` fail-closed optionnel | Passer en fail-closed par dÃ©faut quand `host=0.0.0.0` (`DASHBOARD_REQUIRE_TOKEN=true`). Doc `X-Dashboard-Token`. |
| `control_api_key: '***REMOVED-ROTATE-ME***'` (`config.yaml:178`) | **Non vide** mais **en clair** dans YAML versionnÃ© (diff HEAD) â€” gÃ©nÃ©rÃ© `secrets.token_urlsafe(32)` + portalocker single-flight `tmp+fsync+replace` | **Phase 1 P0** : dÃ©placer hors YAML â†’ `secrets/control_api_key` ou `CONTROL_API_KEY` env `chmod 600` + `gitignore` + rotation doc. Ne pas versionner `config.yaml` avec secret. |
| `0.0.0.0:4000` (`config.yaml:2`, `dashboard/api.py:90-93`) | 9 occurrences `0.0.0.0` | Bindre `127.0.0.1` par dÃ©faut, `0.0.0.0` seulement derriÃ¨re nginx/auth. |
| `PROXY` SSRF (`config/settings.py:29,45,342` + `opencode.py:1339-1405` `proxy=PROXY`) | `PROXY = _env("OPENCODE_PROXY", yaml_get("upstream","proxy",""))` passÃ© direct Ã  `httpx.AsyncClient(proxy=PROXY)` et `_curl_proxy_url(proxy_url)` sans `urlparse`/`scheme` validation ni host allowlist | **Phase 1 P0** : valider `PROXY` schÃ©ma `http/https/socks5` + host allowlist, Ã©chapper `proxy_url` (ne jamais interpoler en shell â€” `httpx` ok mais `curl` subprocess Ã  vÃ©rifier). Test SSRF `PROXY=http://169.254.169.254` bloquÃ©. |
| `docker.sock` mount (`docker-compose.yml:96`) | NÃ©cessaire pour contrÃ´le gluetun | Restreindre : socket proxy (`docker-socket-proxy`) ou `DOCKER_GID` minimal. |
| `cap_add: NET_ADMIN` Ã—4 (`docker-compose.yml:7,111,202,431`) | Requis gluetun | Ok, mais documenter. Pas de `privileged`. |
| `ReadWritePaths` systemd (`opencode.service:15`) | `PrivateTmp`, `ProtectSystem full`, `NoNewPrivileges` | Bien. Ajouter `ProtectHome true` dÃ©jÃ  prÃ©sent. |
| `max_body_size 10485760` (`config.yaml:20`, `nginx:client_max_body_size 10m`) | AlignÃ©. `traffic_capture` `body_cap 131072` (vs `max_body_size 10M`) + `max_bytes 33554432` + `max_frames 500` + redaction `_SENSITIVE_HEADERS` + `_redact_body` query `?api_key` | Ok â€” clamp `body_cap`/`max_bytes` dÃ©jÃ  `max(1024, ...)`/`max(body_cap, ...)`. |
| Secrets gitignored | `.env`, `credentials.env`, `vpn_configs/*.ovpn`, `logs/*.db*`, `.kilo/` | Ok. VÃ©rifier `gitleaks` en CI. |
| `pip-audit` | **0 vuln projet** (`No known vulnerabilities found` â€” vs 32 refresh 14:57) | **âœ… Clos Phase 0** â€” `pip-audit` gate Ã  ajouter en CI pour rester 0. |

---

## 8. Perf â€” dÃ©tail (refresh)

- **Bien :** `orjson` fast-path, `httpx[socks,http2]` pool `max_connections 500`/`max_keepalive 200`/`keepalive_expiry 30` (shared `_client` + `_transport` + `_ensure_http_client()` rebuild si `is_closed`), `uvloop`/`httptools` (non-Windows), DB WAL (`journal_mode WAL`, `synchronous NORMAL`, `cache_size -64000 ~256M`, `mmap 268M` + `busy_timeout 5000` + `commit_interval 5`/`commit_batch 10` + `WAL checkpoint TRUNCATE 3600s` via `asyncio.to_thread`), `gunicorn` `workers cpu_count` `UvicornWorker` `worker_connections 2000` `keepalive 15` `timeout 600`, nginx `proxy_buffering off` SSE + `keepalive 200` + `gzip` JSON + `http2`. Streaming SSE `StreamingResponse text/event-stream` `sse_keepalive 15` `line_buffer_max 1M` `retry_attempts 2` + `finish_reason synthesis` on truncated.
- **Ã€ mesurer / Ã  corriger :**
  - `maybe_reload_custom_routes()` per-request `opencode.py:4925` + poll 5s `_CUSTOM_ROUTES_CHECK_INTERVAL` â€” `os.path.getmtime` synchrone bloque event loop â†’ `run_in_executor` ou `watchdog` + `lifespan` tick, pas per-request.
  - `get_model_config` `lru_cache(512)` + mutation `MODELS` in-place (686,715,980,1006) sans `cache_clear` systÃ©matique sauf 1049 â†’ staleness â†’ invalider systÃ©matiquement ou supprimer `lru_cache` (lookup dict O(1) dÃ©jÃ ).
  - `threading.Lock` chemin async (`_key_cycle_lock`, `_KeyPauser._lock`, `_db_commit_lock`, `_reload_lock`) vs `asyncio.Lock` (`_curl_pool_lock`, `_apply_station_lock`) â€” mix documentÃ© mais Ã  valider sous charge ; `KeyPauser._save` `run_in_executor` fire-and-forget â†’ perte au crash si loop fermÃ©e.
  - `tiktoken` `cl100k_base` `_estimate_input_tokens` via `asyncio.to_thread` partiel â€” ok mais `CACHE_MIN_PROMPT_SIZE 2000`/`max_size 1000` TTL 300 non benchmarkÃ©.
  - `httpx` pool partagÃ© correct mais `_ensure_http_client()` rebuild synchrone â€” ok via `is_closed` check.

---

## 9. Tests â€” inventaire (refresh 15:30)

- **38 fichiers** `tests/` (13 k lignes) : `test_vpn_freshness` 1487 l, `test_gluetun_control_api` 1127 l, `test_proxy` 827 l (+36 l branche), `test_free_multi_attempt` 699 l, `test_invariant_a0` 633 l, `test_vpn_endpoints` 593 l, `test_rotation_concurrency` 552 l, `test_pool_connection_failure` 505 l, `test_env_staleness_guard` 462 l, `test_traffic_capture` 450 l, `test_geo_routing` 437 l, `test_tool_compat` 403 l, `test_free_discovery` 378 l, etc. â€” branche touche 16 fichiers tests.
- `conftest.py` autouse `_reset_disable_mapping` + `pytest_asyncio`.
- Scripts `scripts/smoke_todo10.py` 328 l, `scripts/vpn_e2e_smoke.py` 340 l non gatÃ©s CI.
- **Manques :** streaming SSE rÃ©el, DB WAL, dashboard auth HMAC, rotation N-stations e2e, flaky `asyncio.run + timeout 0.05s + mtime sleep 0.02` (Ã  stabiliser fake clock).
- **VÃ©rif ce refresh 15:30 :** `pytest -o addopts= -q --tb=line --disable-warnings` **611 passed, 1 skipped, 4 warnings in 11.60s** (vs 92% `test_proxy.py` seul refresh 14:57) ; `ruff All checks passed` ; `pip-audit 0`.

---

## 10. Annexes â€” commandes reprodu (refresh 15:30)

```bash
# Tailles & hotspots (refresh 2026-08-23 15:30)
wc -l opencode.py vpn_manager.py dashboard/api.py dashboard/quota.py config/settings.py middleware.py traffic_capture.py protocol_mapping.py free_ip_pool.py shared_rotation.py | sort -rn
# 10433 opencode.py / 4141 vpn_manager.py / 3166 dashboard/api.py / 836 dashboard/quota.py / 1531 config/settings.py / 349 middleware.py / 558 traffic_capture.py / 1593 protocol_mapping.py / 1452 free_ip_pool.py / 389 shared_rotation.py = 24448 core
find . -name "*.py" -not -path "./.venv/*" -not -path "./.kilo/*" -not -path "./logs/*" | xargs wc -l | tail -1
# 42259 total

# Tags historiques
grep -rn "\[plan\|\[fix\|\[incident\|\[audit\|\[Axe\|\[CRITIC" --include="*.py" --include="*.yml" | grep -v ".venv" | grep -v ".kilo" | wc -l
# 341

# Ruff (refresh 15:30)
python -m ruff check . 2>&1
# All checks passed!

# Hygiene git
git status --short | head -30
git diff --stat HEAD | tail -20
# 76 files changed, 1053 insertions(+), 782 deletions(-)
git diff --shortstat HEAD
# 76 files changed, 1053 insertions(+), 782 deletions(-)
git log --oneline -5
# a9bc334 style: ruff format / 08f1108 refactor(audit-phase2-3) / fddac50 fix(audit-phase1) / c3965be fix(ci): lower coverage gate to 35% / 9d2d066 chore(audit-phase1)

# Logs
du -sh logs .kilo
# 6.2G logs / 61M .kilo
ls -lh logs/*.db* | head -20
# 3.1G requests.db / 3.1G bak / 4.1M wal / 32K shm
grep -n "kilo" .gitignore
# 68:.kilo/

# Deps & sÃ©cu
cat requirements.txt
# fastapi==0.141.1 / starlette==1.6.0 / Pillow==12.3.0 / curl_cffi==0.16.1 / rich==14.3.3 / ...
python -m pip_audit --requirement requirements.txt 2>&1 | tail -5
# No known vulnerabilities found  (vs 32 vulns refresh 14:57)
python -m pip_audit 2>&1 | tail -5
# 258 vulns env (pollution browser-use/torch â€” ignorer, scope projet = 32â†’0)

# Docker / gunicorn
docker compose config | head -80
gunicorn -c gunicorn.conf.py opencode:app --check-config

# Tests
python -m pytest -o addopts= -q --tb=line --disable-warnings 2>&1 | tail -5
# 611 passed, 1 skipped, 4 warnings in 11.60s
python -m ruff check . 2>&1
# All checks passed!
```

### Sorties de rÃ©fÃ©rence (2026-08-23 refresh 15:30)

- `ruff` : **0** (`All checks passed!` â€” vs 28 refresh 14:57, vs 29 avant F823, vs 324 totaux historiques).
- `vulture` : non installÃ© (Ã  ajouter).
- `pip-audit --requirement requirements.txt` : **0 vuln** (`No known vulnerabilities found` â€” vs 32 refresh 14:57 : pillow 24 â†’ 12.3.0, starlette 7 â†’ 1.6.0, curl_cffi 1 â†’ 0.16.1, fastapi 0.129.2 â†’ 0.141.1).
- `pip list --outdated` : 30+ majeures en retard (aiohttp 3.13â†’3.14, anthropic 0.46â†’1.0, etc.) â€” pinner avant bump.
- `gitleaks` : non installÃ© (Ã  ajouter).
- `git diff --stat HEAD` : **76 files changed, 1053 insertions(+), 782 deletions(-)** (vs 72 files 788/567 refresh 14:57).
- `wc -l` core : **24448** (vs 24560 refresh 14:57 â€” -112 l net aprÃ¨s dÃ©duplication) â€” `opencode.py` 10433 (-75), `protocol_mapping.py` 1593 (-47).
- `logs/` : **6.2G** (inchangÃ©) â€” `requests.db` 3.1G + `bak` 3.1G + `wal` 4.1M ; `.kilo/` 61M dÃ©sormais ignorÃ©.
- `grep tags` : **341** (vs 339 refresh 14:57, vs 342 baseline).
- `pytest` : **611 passed, 1 skipped, 4 warnings in 11.60s** (full suite, `addopts` neutralisÃ©).

---

## 11. DÃ©cision & next step (refresh 15:30)

**Gate actuel :** Phase 0 **clos** â€” `ruff 0`, `pip-audit 0`, `pytest 611 passed`, `server_countries` source unique.

**Faits ce refresh 15:30 :**
- âœ… `ruff All checks passed` (28â†’0 : F401/F811/E402 + F841 Ã—3 `has_reasoning_item`/`has_thinking_block` `protocol_mapping.py:1228,1285-1290`)
- âœ… `pip-audit 32â†’0` (Pillow 10.4.0â†’12.3.0, starlette 0.52â†’1.6.0, curl_cffi 0.14.0â†’0.16.1, fastapi 0.129.2â†’0.141.1)
- âœ… `server_countries` source unique F-H5 â†’ `config.yaml` â†’ `docker-compose.yml ${SERVER_COUNTRIES:?...}` fail-closed + `scripts/make_credentials_env.py`
- âœ… `tests/test_vpn_e2e.py` `AsyncMock` fix + `protocol_mapping` F841 â€” `pytest 611 passed` vÃ©rifiÃ©
- âœ… `core 24448` (-112 l) / `total py 42259` / `tags 341` / `git 76 files 1053/782`
- â³ Reste P1 : `PROXY` validation `urlparse` + allowlist (F-H6/SSRF), `DASHBOARD_REQUIRE_TOKEN` fail-closed, `control_api_key` hors YAML (`chmod 600`), DB rotation 6.2G, `threading.Lockâ†’asyncio.Lock` + `lru_cache` staleness

**Next step recommandÃ© :** commit Phase 0 `chore/audit-phase0: ruff 0 + pip-audit 0 + server_countries single-source + F841` puis **Phase 1 P0 sÃ©cu** (`PROXY` SSRF, `DASHBOARD_REQUIRE_TOKEN`, `control_api_key` hors YAML) en PR `fix/audit-p1-security`.

*Rapport refresh gÃ©nÃ©rÃ© le 2026-08-23 15:30 â€” versionnÃ© dans `docs/audit-2026-08-23.md` + `docs/dead-code-2026-08-23.md`.*
