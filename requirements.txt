# Pinned 2026-08-23 — generated from ranges via `pip freeze` / `pip index versions`
# Source unique: requirements.txt (requirements.lock removed — see audit F-H7)
# To regenerate with hashes: `pip install pip-tools && pip-compile --generate-hashes requirements.in -o requirements.txt`
# or `uv pip compile requirements.in --generate-hashes -o requirements.txt`
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.41.0
httpx[socks,http2]==0.28.1
rich==14.3.3
tiktoken==0.12.0
pystray==0.19.5
Pillow==12.3.0
pywebview==6.1
pyyaml==6.0.2
duckduckgo-search==7.5.5
beautifulsoup4==4.12.3
trafilatura>=1.8,<3
curl_cffi==0.16.1
orjson==3.11.7
uvloop==0.22.1; sys_platform != "win32"
httptools==0.7.1
redis==6.4.0
gunicorn==22.0.0
